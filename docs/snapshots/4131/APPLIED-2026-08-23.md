# #4131 APPLIED i prod 23/8 kl. 14:45-15:05 (ejer-go: "kalenderen først")

1. `moveRacesOffFinalMonday4131.mjs --apply`: 25 endagsløb flyttet fra 21/9 (snapshot `moved-2026-08-23.json`). Første kørsel fejlede på tidsformat (`+00:00Z`), rettet i scriptet (slice(11,19)), anden kørsel skrev 25 rækker.
2. Fund efter apply: 8 af de 25 beholdt `game_day_start=27` (sibling-kopien fandt ingen sibling). Rettet via SQL: `game_day_start = scheduled_for::date - '2026-08-25'` (invarianten holdt for de øvrige 463).
3. Fund: 16 ETAPER lå stadig på 21/9 (11 etapeløb der starter 19-20/9: Vuelta Ibérica D1, Tour Adriatique D2 x2, Région Pays de la Loire Tour Mineur D4 x8). Scriptet kiggede kun på løbets startdato. Løst via SQL: de 11 løb + Tour de l'Hexagone (D1, for at undgå to GT'er samme dag 16/9) forskudt -1 dag på alle etaper (`race_stage_schedule.scheduled_at/game_day`, `races.scheduled_for/game_day_start`). Før-tilstand gemt i `backup_races_4131b_20260823` (20 rækker) + `backup_rss_4131b_20260823` (76 rækker).
4. `seasons.race_days_total` = 27 (distinct game_day_start, samme regel som `recomputeSeasonRaceDays`).
5. Post-verify `verifySeason3Calendar.mjs`: GO (471 løb, 27 dage, sidste løbsdag søndag 20/9, maks 4 GT-etaper/dag, ingen dag med to GT'er). GT-vinduer: Giro 30/8-4/9, Tour 11-15/9, Vuelta 16-20/9.

Gendannelse: de to før-tabeller + `moved-2026-08-23.json` dækker alle 37 rørte løb.
