# S1-S5-bølge 23/7 (dagbølge, Fable-orkestrator + sonnet-workers)

| Metrik | Værdi |
|---|---|
| Start/slut (lokal tid) | 11:30 → ~15:00 |
| Agenter launched / fuldført / døde | 10 (5 workers + 4 adversarielle reviewers + 1 respawn) / 9 / 1 (S4 r1 hang 85 min, dræbt) |
| PR'er åbnet / merged | 4 + 1 orkestrator-PR / 3 merged (#2834, #2837, #2841) + 3 draft afventer ejer (#2829, #2831, #2832) |
| Prod-applies | S4-konvertering (14 rækker, post-verify vandtæt, scorecard GO) |
| Issues → claude:done | #2744, #2748, #2755 |
| Issues oprettet | #2830 (131 tabeller brede grants), #2833 (oprykningsbesked-sprog), #2835 (team_id ved pension → fixet af ejer-session i #2839), #2836 (sælger-gulv ved auktions-finalisering), #2838 (regionOf-regex) |
| Preflight | GO kl. ~11:32 (`.codex.local/night-wave-preflight.json`), keep-awake kørt hele bølgen |

## Præmis-målinger der ændrede sig (guardrail 1+3+5)

- **S5/pension:** audit sagde 38 ryttere 36-39 + 0 på 40+ (menneskehold). Spillets egen aldersformel evalueret ved sæson 2 (som koden gør): **81 + 11 garanteret pension**. 38 hold under 12-norm (ikke 13). Sikkerhedsgulvet holdt: min worst-case = 8, 0 hold under 8. → varsel-`--live` er ejer-gated.
- **S3/sponsor:** "65/70 med gammel divisor" var reelt **52/70**.
- **S3/løn:** `check_salary_drift`-RPC'en fandtes ikke — backfill genimplementerer `computeFrozenSalary` i SQL (18/18 paritetstest).
- **S4:** D3 har **4 puljer**, ikke 8 (1/2/4/8-pyramiden). #2825's S2-prompt pegede på #2164, som reelt er D3→D4-nedrykning (åben, S8-relevant) — sprog-fixet er nu #2833.

## Adversarielle reviews (guardrail 2) — fund der blev fanget FØR merge/apply

- **S1:** role-triggeren ville have låst operatør-/migrations-vejen ude (`auth.role()` er NULL uden PostgREST) + REVOKE manglede REFERENCES/TRIGGER. Fixet.
- **S2:** total udsendelses-fejl 27/7 ville være usynlig (ingen captureException, toast læste aldrig resultatet) + 3 testkonti i modtagerkredsen + deep-link landede på tom ny sæson. Fixet (150 modtagere, link → afsluttet sæson).
- **S3:** IEEE-754-afrunding i sponsor-inversen perturberede 1 allerede-korrekt række. Fixet (heltals-afrunding af udledt target).
- **S5:** CI reelt rød (swallowed-catch), 3 fejlkoder uden da-oversættelse, "hovedtest 2" beviste intet (afkoblet fra kode), dobbelt-fradrag af pending-out-ryttere. Alt fixet; 2 huller → #2835/#2836.
- **S4:** `shadowRace` manglede `name` → 10/14 rækker ville have fået generiske stigningsnavne, frosset af `is_manual=true` ved rutedata-lås 27/7. Fixet før apply; per-pulje-paritet (1 bjergklassiker/pulje i alle tiers) valgt over rå 8,3 %-andel.

## Afvigelser/læringer

- **S4 r1 hang** 85 min med 0 output; ping ubesvaret → dræbt + respawnet med konkrete script-pointers og commit-tidligt-disciplin → leverede på ~30 min. Læring: giv workers verificerede fil-pointers til regen/scorecard-mønstre UPFRONT; worktree-fremdrift (commits) er eneste pålidelige liveness-signal.
- **Chips:** en worker spawnede en ejer-chip (#2835) → ejer-regel: orkestrator ejer opfølgninger; workers må ikke spawne chips (memory: `feedback_orchestrator_owns_followups_no_owner_chips`).
- **Delt checkout:** S3-revieweren observerede et kortvarigt branch-flip i hoved-checkoutet (uforklaret; tilstand verificeret ren bagefter). Reviewer-praksis "genimplementér fra `gh pr diff`, stol ikke på live-reads" virkede som designet.
- **`npx --prefix frontend playwright`** fra repo-rod loader IKKE frontend-configen (tom projektliste, manglende baseURL) → kør altid fra `frontend/`. To mobile-webkit-flakes under load (navigation-race) — grønne isoleret.
- **audit-CI'en (Feature-liveness)** var rød på alle PR'er (data-drevet, pre-eksisterende, NOW.md-dokumenteret) — behandlet som ikke-blokerende efter reviewer-bekræftelse af nul filoverlap.
