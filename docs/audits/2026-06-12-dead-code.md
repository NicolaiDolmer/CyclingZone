# Dead-code-audit — 2026-06-12 (dagbølge 3, audit:dead-code)

Reference-tælling af alle ikke-test-filer i `frontend/src`, `backend/` og `scripts/` mod hele det trackede tekst-korpus (kode, package.json-scripts, CI-workflows, hooks, Playwright-config, .ps1/.sh, docs). Dynamiske imports, lazy routes, cron-jobs og workflow-kald tæller som brug (basename-match fanger `import('./pages/X')`-mønstre). Dertil export-niveau-scan (word-boundary) for `frontend/src` + `backend/lib`.

**Hovedresultat:** `frontend/src` er rent på fil-niveau — alle 204 filer har mindst én ægte forbruger (typisk siden/route der renderer dem). Dødvægten ligger i engangs-scripts og én superseded backend-lib.

## Fjernet i denne PR (100 % entydige, alle 0 kode-referencer)

| Fil | Evidens |
|---|---|
| `backend/lib/linalg.js` + `linalg.test.js` | v1 ridge-regression-hjælper til værdimodellen. v2 (commit `b9074bfc`, 7/6) fjernede al brug; `riderValuationFit.js` har egen `olsSolve` ("generel numerik er bevidst fravalgt — YAGNI"). Eneste reference = egen test. |
| `backend/scripts/simulateSeason1Calendar.js` | Selverklæret "Engangs-simulation" med indlejret pool-snapshot fra 2026-05-19 (stale). Formål (verifikation før admin-UI-brug) opfyldt 19/5. Afløst af `simulateSeasonDryRun.js` + relaunch-rehearsal. |
| `scripts/file-discord-issues-batch3.js` … `batch9.js` (7 filer) | Engangs issue-filing-scripts med hardcodet issue-indhold; alle eksekveret (issues #155-#176, #775-788, #834, #991, m.fl. eksisterer). Re-kørsel ville skabe dubletter. `batch10.js` (nyeste, 6/6) beholdt som template for næste sweep. |
| `scripts/analyze_missing_riders.mjs`, `analyze_new_riders.mjs`, `relookup_missing.mjs`, `insert_new_riders.mjs` | World-db diff/import-tooling for RIGTIGE ryttere (#964, 2/6) — selverklæret one-off, eksekveret. Obsolet ved relaunch-beslutningen 20/6 (fiktive ryttere, epic #1105); real-rider-pipelinen demonteres parallelt (#1207). |
| `formatAuctionEnd()` i `backend/lib/auctionEngine.js` | Dansk display-formatter i backend — 0 referencer overhovedet (heller ikke i tests, heller ikke internt i filen). UI formaterer selv tid. |

Alt kan genfindes i git-historik.

## Verificeret LEVENDE trods få/0 referencer (beholdt med vilje)

- **`backend/lib/racePointModel.js`** — kun refereret af egen test, men det er pointen: filen er den data-agnostiske JS-reference for SQL-kaskaden (`database/2026-06-01-race-point-model.sql`); regressionstesten ER forbrugeren (SQL-paritetskontrakt, #894).
- **Ops-/launch-tooling (manuel kørsel = 0 statiske refs er forventet):** `executeSeasonTransition.js`, `simulateSeasonTransitionDryRun.js` (#1155-transition-værktøj, juni), `seedRacePool.js` (idempotent re-seeder), `responseCacheLoadCheck.js` (perf-harness), `backend/scripts/dev/run-relaunch-rehearsal.mjs` (aktiv launch-prep 11/6), `scripts/loadtest/*` (brugt i audit 11/6).
- **Discord-tooling:** `dump-threads.mjs`, `dump-new-threads.mjs`, `read-channels.mjs`, `list-active-threads.mjs`, `scan-old-domain.mjs` (oprettet 11/6), `check-discord-bot-token.mjs`, `sync-discord-attachments.js` — aktive sweep-/diagnose-værktøjer (Discord-sweep er obligatorisk før board-arbejde). De seks `discord-i18n-step-*.sh`/`discord-fase1-finish.sh` er eksplicit dokumenteret som beholdte templates i `scripts/discord/README.md`.
- **`scripts/brand-contrast-check.mjs`** (brand-tooling, doc-refereret), **`scripts/time-tracker/backfill-labels.mjs`** (re-kørbar label-backfill, #390).
- **`backend/scripts/clampFictionalRiderStats.js` + `.backup.json`** — engangs-reparation (#1122) allerede kørt, MEN backup-filen er eneste rollback-vej for live fiktive ryttere indtil relaunch 20/6. Slet begge efter relaunch.

## Ejes af parallelt spor — bevidst IKKE rørt her

Sheets/UCI/Excel-demonteringen (#1207 m.fl., kører i denne bølge): `backend/lib/sheetsSync.js` (`syncUCIPoints` har 0 refs), `dynCyclistSync.js` (`syncDynCyclist` har 0 refs), `raceResultsSheetSync.js`, `adminImportUpload.js`, `pcm*`-filerne, `backend/scripts/verifyRidersAgainstSheets.js`, `scripts/sync-dyn-cyclist-teams.mjs`, python-scrapers. Desuden NO-TOUCH-listen (training*/riderProgression*/raceSimulator/cron-trænings-jobs) og `relaunchOrchestrator.js`/`relaunchSeason1.js` (verificeres af parallelt spor).

## Export-niveau-fund (rapport-only — ingen handling i denne PR)

144 exports uden eksterne ikke-test-referencer. Langt de fleste er bevidste test-seams (mønsteret `export` + kun egen test bruger dem — fx `deadlineDayReport`, `boardConsequences`, `pricing.js`) eller info-konstanter (`SALARY_RATE_INFO` er eksplicit "Info-only" og spejler en DB-GENERATED-formel). Ingen af dem er værd at fjerne maskinelt. De få med 0 referencer overhovedet, som KUNNE trimmes ved lejlighed (lav værdi, ikke 100 % entydige):

- `frontend/src/lib/uciRaceClasses.js` → `MAX_RANKS` (datatabel, kun definition)
- `frontend/src/lib/consent.jsx` → `CONSENT_CATEGORIES` (semantisk dokumentation af consent-kategorier)
- `frontend/src/lib/ridersUrlState.js` → `RIDERS_FILTERS_SESSION_KEY`-eksporten (bruges internt)
- `backend/lib/adminImportUpload.js` → `ADMIN_IMPORT_MAX_FILES` (fjernes alligevel med Excel-import-sporet)
- `frontend/src/pages/HallOfFamePage.jsx` → `export` på `getLevelInfo` (bruges kun internt)
- `frontend/src/lib/boardCopy.js` → `resolveBoardCopy` (0 refs i dag — men board-i18n-sporet er aktivt i denne bølge; lad det spor afgøre)

## Metode-noter

- Scan: basename-match over alle trackede tekstfiler (fanger statiske + dynamiske imports, CLI-kald i .yml/.ps1/.sh, package.json-scripts). Falske negativer mulige ved navne-kollision (fx `boardUtils` findes både i frontend og backend) — derfor blev HVER fjernelseskandidat efterverificeret med målrettet grep + git-log (hvem importerede den historisk, og hvilken commit fjernede brugen).
- `scripts/migrations-manual/*.sql` og arkiv-docs er bevidst udeladt (dokumentation af hvad der er kørt, ikke kode).
