# Game invariants (stabile konstanter)

Stabile rules der sjældent ændrer sig. Hvis du redigerer økonomi-kode, race-resultat-flow eller upload-håndtering — tjek herinde først.

Flyttet fra `NOW.md` 2026-05-14 (Phase 4 af `scalable-wobbling-blossom`) for at holde NOW.md som ren ephemeral status.

## Kritiske invarianter

- **Verificér runtime før claims** — runtime > docs. Doks går stale; koden er sandheden.
- **Economy-konstanter** (backend SSOT: `backend/lib/economyConstants.js`):
  - `DEFAULT_BETA_BALANCE = 500_000` (#1717, 2026-06-22)
  - `SPONSOR_INCOME_BY_DIVISION = { 1: 600_000, 2: 400_000, 3: 340_000, 4: 315_000 }` (E2 strict_fair_v1; D3 hævet 260K→340K + D4 tilføjet i #1441 Fase 1 — rettet 4/8 efter sponsor-audit, linjen stod stale på 260K uden D4) — sponsor-base er division-skaleret og AUTORITATIV i `sponsorEngine.computeSponsorForSeason`; `SPONSOR_INCOME_BASE = 240_000` er nu kun legacy fallback-gulv (ukendt division). Sæson 2+ bruger SAMME division-base + en performance-pulje `VARIABLE_SPONSOR_POOL = 150_000` (0-150k oveni ud fra forrige sæsons point/rank). Den tidligere flade `VARIABLE_SPONSOR_BASE = 2.500.000` er fjernet 2026-06-17 ([#1439](https://github.com/NicolaiDolmer/CyclingZone/issues/1439); band-aid fra open-beta → ren inflation efter E2). **Hård grænse: maks sponsor-udbetaling = 900.000** (D1 top-gross 750k × board-modifier-loft 1,2). Fuldt økonomi-redesign: epic [#1441](https://github.com/NicolaiDolmer/CyclingZone/issues/1441).
  - `SALARY_RATE = 0.067` (E2 strict_fair_v1, ×0.67 af den gamle 0.10) — senior-kontrakter, delt SSOT i `economyConstants.js`, importeret af `contractSeed` + `marketUtils`. Akademi/ungdom har sin EGEN rate (`academyFlag.ACADEMY.SALARY_RATE = 0.10`, urørt).
  - **Rytter-værdi (#1101 cutover 2026-06-10):** `market_value = COALESCE(base_value, 1000) + prize_earnings_bonus` — GENERATED i DB (`database/2026-06-10-value-cutover-base-value.sql`). `base_value` skrives KUN af backfill/relaunch-orchestrator (model v3: `riderValuationModel.json`). `uci_points` er afkoblet og må ikke vises player-facing eller indgå i værdi-formler. Fallback-konstanten 1000 spejles i `marketUtils.RIDER_BASE_VALUE_FALLBACK` + frontend `marketValues.js`. Audit: `backend/scripts/auditValuationCutover.js`.
  - **`riders.salary` er FROSSEN ved signering (#1309 cutover 2026-06-13):** kolonnen er IKKE længere GENERATED — den skrives én gang ved erhvervelse (auktion-win, transfer-accept, seed) og ændres ikke automatisk bagefter. Formel ved signering: `max(1, round(market_value × SALARY_RATE))` (E2: 0.067). Kontraktfelter: `contract_length` (1-3 sæsoner, INTEGER) og `contract_end_season` (INTEGER). Invarianter:
    - **Ejede ryttere (team_id != null) har ALTID en kontrakt** (salary != null, contract_length != null, contract_end_season != null) — garanteret af relaunch-seed (#1309) + contract-on-acquire på alle erhvervelsespaths (auktion, transfer, swap).
    - **Free agents (team_id null) har NULL salary** — UI estimerer via `resolveRiderSalary` / `getRiderSalary` (~6,7% af market_value) udelukkende til visningsformål. *Fuldt realiseret når #1310-markedspakken lander:* erhvervelsessiden garanterer i dag at ejede ryttere altid har salary != null (seed + on-acquire), men frigivelsessiden (squadEnforcement auto-salg til fri-agentur, admin override→null) nulstiller endnu ikke salary — en netop frigivet rytter kan derfor bevare en harmløs frossen løn (identisk med hvad resolveren ville estimere, da base_value er frosset). Release-side-clearing er en del af #1310.
    - **Kontrakt arves uændret ved handel** — salary, contract_length og contract_end_season nulstilles/genberegnes ALDRIG ved transfer eller auktions-ejerskiftssalg; køber overtager eksisterende kontrakt som-er.
    - Contract flows (fornyelse, udløb→auktion, release, re-signing-formel) er fast-follow i markedspakken (#1310).
  - **Akademi (#1308, cutover ved relaunch 2026-06-20):**
    - **Akademiryttere (`is_academy=true`) tæller ALDRIG mod senior 30-cap** og auto-sælges ALDRIG af squad-enforcement. De udelukkes fra alle senior-squad-cap-tællinger og løb-berettigelse, men INDGÅR i daglig træning (med ungdoms-multiplikator fra `ACADEMY.YOUTH_MULTIPLIER`).
    - **8-plads akademi-cap håndhæves på ENHVER akademi-tilføjelse** (signering, auktionsgevinst, fri-agent-signering). Backend afviser hvis `academy_count >= 8` på holdet (`academy_full`).
    - **Ungdomsauktion (Fase B, `auctions.is_youth=true`) har INGEN sælger** (`seller_team_id=NULL`): en afvist intake-kandidat. Vinderen optager rytteren i SIT akademi (`is_academy=true`, ungdomskontrakt) og betaler sit bud som `academy_signing` — en SINK (ingen seller-payout, ingen rytter går retur til en sælger). Akademi fyldt / for lav balance → auktionen annulleres og rytteren forbliver fri ungdom. Ingen bud → fri ungdom (`team_id=NULL`). Akademiryttere bypasser transfervindue-pending (sættes direkte på vinderen).
    - **Akademi-drift = `ACADEMY.DRIFT_PER_SEASON` (5 000 CZ$) per besat akademi-slot**, opkrævet ved sæsonlønkørslen (`processSeasonStart`), idempotent per sæson+hold via `academy_drift`-finance-type. Drift gælder kun hold med `academy_enabled=true`.
    - Feature-flagget `academy_enabled` er OFF indtil relaunchen; al akademi-kode er gated bag flaget og må IKKE aktiveres uden ejer-godkendelse.
  - `PRIZE_PER_POINT = 75` — race_results.prize_money = points × 75 i alle resultat-stier (race-motor, PCM-fallback, approve-results). Backend importerer fra `economyConstants.js`; frontend har sin egen kilde (`frontend/src/lib/expectedPrizeCalculator.js` + `rulesNumbers.js`) — de to codebaser kan ikke dele import, så hold dem i sync. **#1816 (2026-06-23): sænket 1500 → 75 (÷20).** Det gamle niveau gjorde præmie til den dominerende pengekilde (D1 ~2,2M/sæson = 3× sponsor, ~14× #1441-break-even-antagelsen); 75 gør præmie til et supplement (~30-40% af sponsor). Rolle: præmie = sekundær bonus, sponsor = primær indtægt. Eksisterende prod-præmie blev retroaktivt re-skaleret ÷20 (`database/2026-06-23-prize-money-rescale-divide-20.sql`).
  - **Stjernerytter ([#1205](https://github.com/NicolaiDolmer/CyclingZone/issues/1205)/[#1210](https://github.com/NicolaiDolmer/CyclingZone/issues/1210)):** `STAR_RIDER_MARKET_VALUE = 8_000_000` — en rytter er stjerne når `market_value >= 8M CZ$`. Delt diskriminator for force-sale-beskyttelsen (boardConsequences lag 4, "star riders protected" i help) og `team_star`-achievementet; `transfer_bargain` = købt for `< market_value / 2`. Re-kalibreret 2026-06-10 mod fiktiv launch-population (post-#1209): ≥8M = 12 af 800 (1,5%) = superstjerne-båndets grænse (ejer valgte A på #1210; oprindelig 5M-kalibrering mod PCM-prod ramte 2,5% efter re-tunen). Ændres tærsklen skal player-facing copy følge med (`locales/en+da/achievements.json` + achievements-DB-fallback + denne fil). `uci_points` er frosset og må ikke bruges i nye live paths (eneste rest: `boardIdentity.calculateRiderStarScore`, se #1205 out-of-scope).
  - **Gældsloft (division ceiling):** `DEBT_CEILING_BY_DIVISION = { 1: 1_200_000, 2: 900_000, 3: 600_000, 4: 400_000 }` (`backend/lib/economyConstants.js:105` — kanonisk kilde; D4 tilføjet med 4-divisions-pyramiden, denne linje stod på D1-D3 uden D4 indtil #4282). Nødlåns-gældsgrænse = division ceiling (IKKE flad 1.5M; supersederer evt. ældre notat om 1.5M safety-net — [#1441](https://github.com/NicolaiDolmer/CyclingZone/issues/1441) reverterede det). Nødlån er hard-clamped: du kan ikke låne dig over loftet. **Måles mod `interestExcludedDebt` (`amount_remaining - accrued_interest`, samme størrelse som `economyEngine.js:783` bruger til at afgøre frysning), IKKE rå `amount_remaining`** — et hold der maxer et lån bryder ellers loftet så snart første rente kapitaliseres, uden at have gjort noget ([#4282](https://github.com/NicolaiDolmer/CyclingZone/issues/4282), fixet i `verify-invariants.js`s `debt_within_ceiling`-check af PR #4291, 2026-08-27).
  - **Upkeep (gold sink, #1441 Fase 1):** `UPKEEP_BY_DIVISION = { 1: 220_000, 2: 70_000, 3: 20_000, 4: 0 }` — flad løbende driftsomkostning pr. division, opkrævet ved sæsonstart (`processSeasonStart`). Idempotent pr. sæson+hold. Ikke modifier-skaleret. Finance-type `"upkeep"`, `metadata.code = "tx.upkeep"`. _Halveret + D4-fritagelse af ejeren 23/8, låst i #4151; denne linje stod på de gamle tal (440/140/40, ingen D4) indtil 25/8. Kilden er `backend/lib/economyConstants.js:56`._
  - **Sponsor-payout-loft (Fase 1):** `FINAL_SPONSOR_PAYOUT_CEILING = { s2plus: 900_000, s1: 720_000 }` — hård maksimal sponsor-udbetaling EFTER board_modifier × pullout. Sæson 2+: 900K · Sæson 1/intro: 720K. Gælder uanset division eller modifier.
  - **D3 sponsor-base hævet:** 260K → 340K (Fase 1, [#1441](https://github.com/NicolaiDolmer/CyclingZone/issues/1441)) for at holde D3 bæredygtigt mod det nye upkeep-sink. `SPONSOR_INCOME_BY_DIVISION = { 1: 600_000, 2: 400_000, 3: 340_000, 4: 315_000 }` (D4 = 315K, tilføjet med 4-divisions-pyramiden).
  - **Eskalerende gældhåndhævelse (Fase 1):** Har et holds samlede gæld overskredet sit divisions-loft ved sæsonslut, sættes `transfer_frozen = true` (ingen bud/transfer/swap/lån). Holder gælden sig over loftet én sæson mere (2 på hinanden følgende sæsoner over loftet) sælges holdets dyreste rytter tvungent (`forced_debt_sale` finance-type) og tilgodehavendet krediteres holdet; ingen `metadata.code` på den transaktion (falder tilbage på description).
  - **Faciliteter/staff ([#1441](https://github.com/NicolaiDolmer/CyclingZone/issues/1441) Fase 3 A1):** `FACILITIES_ENABLED = false` (kode-konstant, `backend/lib/facilityConstants.js`) — hele featuren (køb, ansæt, payroll-sinks, API) er død indtil bølge A2-harness er grøn OG ejer siger go. Staff-tier ≤ facilitets-tier (håndhævet i `validateHire`). Kommerciel facilitet: tilbagebetalingstid ≥ 4 sæsoner (`COMMERCIAL_MIN_PAYBACK_SEASONS`, harness-gate i A2). Alle facility/staff-pengebevægelser skriver `finance_transactions`-rows via `debitTeam` med idempotency-keys (`facility_purchase:{team}:{track}:{tier}`, `staff_severance:{team}:{role}:{staffId}`, `facility_upkeep:{team}:{season}`, `staff_salary:{team}:{season}`). Alle pris-konstanter er START-KANDIDATER indtil A2-kalibrering.
  - **Tid-som-valuta (designdisciplin, spec 2026-07-05):** alle fremtidige økonomi-priser prissættes i "sæsoner af repræsentativ indkomst" for mål-segmentet og kalibreres i harness — gættes aldrig direkte i CZ$.
- **Fri/AI-ryttere & præmie:** præmie optjent af ryttere uden manager-hold tæller med i deres `market_value` (`updateRiderValues` summerer `prize_money` pr. rytter **uden** team-filter via `prize_earnings_bonus`), men **udbetales aldrig** til et hold. Det er bevidst: holdsløse ryttere stiger i værdi, men ingen modtager pengene. **Salary er ikke berørt** — salary er frossen ved signering (#1309) og genberegnes ikke fra præmier; fri/AI-rytteres salary er NULL (resolver estimerer ~6,7% af market_value til visningsformål).
- **Rytter-værdi genberegnes ved sæson-slut OG ved præmie-udbetaling** (R3, [#895](https://github.com/NicolaiDolmer/CyclingZone/issues/895)). `prize_earnings_bonus` = **progress-vægtet gennemsnit** over de op til 3 nyeste sæsoner: `round(Σ earnings_s / max(Σ w_s, 1))`, hvor en `completed` sæson vejer 1 og den aktive sæson vejer sin fremgang (`race_days_completed / race_days_total`, clamp 0..1). `max(…, 1)`-gulvet forhindrer at en alene-stående aktiv sæson (fx beta-sæson 1 uden completed-anker) annualiserer én tidlig præmie til en oppustet værdi. Uden aktiv sæson reducerer formlen bit-for-bit til den gamle "snit over completed sæsoner". Trigges fra `processDivisionEnd` (sæson-slut) **og** `paySeasonPrizesToDate` (admin-udbetaling). Design: [`docs/slices/prize-money-audit-r3-design.md`](slices/prize-money-audit-r3-design.md).
- **race_results.result_type — kanonisk for endags-løb = `gc`** (ikke `stage`). Matcher XLSX-stien ("general results"→gc) + engine-opslaget `single: {gc: "Klassiker"}`. result_type er kun en label; præmie afhænger af `race_points`-opslaget, ikke result_type.
- **Auction finalization** har parallelle paths i `api.js` og `cron.js`; begge skal delegere til `auctionFinalization.js` — ikke duplikere logik.
- **Løbs-afslutning er en KÆDE, ikke én handling** ([#4147](https://github.com/NicolaiDolmer/CyclingZone/issues/4147), 4/9). `simulateStageByIndex` kører trinnene i fast rækkefølge, og `races.finalize_state` (jsonb) markerer hvert færdigt trin for præcis den etape der afsluttes. Ingen igangværende afslutning = markeringen er `NULL`; det er den eneste tilstand et sundt løb hviler i. Bag `app_config.race_finalize_resumable_enabled` (default OFF). Kontrakt + trin-navne: `backend/lib/raceFinalizeState.js`.
  - **Hvad et SIGTERM midt i hvert trin efterlader** (drain-vinduet er 120 s i `server.js` + `drainingSeconds=150` i `railway.json`, #4150 — en afslutning tager 90-110 s målt, så den normale sag er at kæden når i mål):

    | Trin | Dør processen her | Genoptagelse |
    |---|---|---|
    | `write` | Ingen delvis tilstand: `apply_stage_result` er ÉN Postgres-transaktion (counter-bump + `race_results`). Enten alt eller intet (#1598). | Uden markering: etapen køres helt om. Med markering: springes over. |
    | `standings` | `race_results` er korrekte; `season_standings` er stale. | Fuld re-derivation — køres om. Markeres KUN ved succes. |
    | `matview` | Ranglister er stale (op til 10 min, indtil cron-fallbacken). | Idempotent REFRESH — køres om. |
    | `enrichment` | Etapen mangler helt eller delvist `race_simulation_runs`/`race_incidents`/`race_stage_moments`/tidslinjer. **Dette var det stille hul:** counteren var bumpet, så etapen kunne aldrig tages op igen. Målt i prod 4/9: 34 etaper (S1 29, S2 4, S3 1) uden ét eneste sim-run. | Alle skrivninger er delete-then-insert scopet til etapen, og simuleringen er seedet (`stableSeed(raceSeedInput(race.id, stageNumber))`) → en genkørsel skriver præcis de samme rækker. |
    | `fatigue` / `rest-day` | Trætheden er enten skrevet eller ikke — aldrig halvt (ét upsert-kald pr. felt). | **ENGANGS-trin:** markeres efter FORSØGET, ikke efter succes. De akkumulerer på `rider_condition.fatigue`; en manglende skrivning er langt billigere end en dobbelt. |
    | `board` | `race_days_completed` og board-weekend kan være halvt anvendt. | `recomputeSeasonRaceDays` er re-derivation og board-weekenden differ previous-vs-new → køres helt om. |
    | `notify` | Discord-embed og/eller in-app-besked er sendt — eller ikke. | **ENGANGS-trin.** Markeres efter forsøget, så en genoptagelse aldrig giver managere to beskeder. Uden markering (flag OFF) springes notify helt over i recovery — så et løb der crashede her fortalte aldrig nogen at det var kørt. |
    | `status-flush` | `status` er stadig ≠ `completed` — præcis Llanera-tilstanden 23/8 (resultater skrevet, præmier beregnet, status aldrig flippet). | Sidste trin, sat efter alt andet. Både den grovkornede `finalizationPending`-recovery (5-min scheduler-tick) og markeringen tager den op. |
    | efter alle trin, før rydning | Løbet er `completed` MED en efterladt markering. | Genoptagelsen ser at alle trin er færdige, rydder markeringen og rører intet andet. |

  - **Præmier kan ikke dobbeltudbetales af en genoptagelse.** Udbetalingen ligger UDEN FOR kæden (`autoPrizeSweep` → `prizePayoutEngine.paySeasonPrizesToDate`) og er dobbelt-beskyttet: `races.prize_paid_at`-CAS'en (#1573, `UPDATE … WHERE prize_paid_at IS NULL` med read-back) og `uniq_finance_idempotency_key` på `race_prize:<race>:<team>`. Et genoptaget løb bliver berettiget til den ENE udbetaling det manglede — aldrig to.
  - **Vagt:** `runHalfFinalizedRaceWatch` (`backend/lib/raceFinalizeWatch.js`, cron hvert 15. min, READ-ONLY) alarmerer via Sentry med fast fingerprint `race-finalize-half-state` + `opsAlertDedupe` ved: markering der har stået stille >10 min uden levende stage-claim; alle etaper kørt men status ≠ `completed` >10 min; `completed` uden `prize_paid_at` >60 min trods præmie-berettigede rækker (kun når auto-prize er tændt). Den reparerer intet.
- **AI/bank/frozen får aldrig board-state** — board features er manager-only.
- **`board_plan_snapshots = 0` er FORVENTET, ikke en fejl (#2596):** tabellen er et sæson-slut-retrospektiv (skrives af `processTeamSeasonEnd`/`buildSeasonEndPreviewRows`), ikke spillerens live plan — den forbliver tom indtil første reelle sæson-slut for en 1yr/3yr/5yr-plan er kørt. `loadGoalContextForBoard` (`boardGoalContext.js`) håndterer tom-tabel-casen korrekt (falder tilbage til `null`-baseline → mål-evaluatorer returnerer `awaiting_data`). Forveksl den ikke med et brudt write-path (#2463 var tæt på denne fælde).
- **Admin-resultatupload** — eneste upload-path er PCM-fallbacken `/api/admin/import-results-pcm` (multipart `files[]`, maks 30 filer, Excel-filer i memory med 10 MB loft pr. fil). Excel- og Sheets-import + dyn_cyclist-/UCI-sync er fjernet 2026-06-12 (#1179/#1180/#1207) og må ikke genindføres — forward-guard i `backend/lib/adminRouteOwnership.test.js`.
- **PostgREST's TAVSE 1000-rækkers-loft (#3331):** ethvert `supabase.from(table).select(...)` uden `.range()` returnerer stiltiende MAKS 1000 rækker — ingen fejl, ingen advarsel, bare et trunkeret resultat. Har bidt to gange: PCM rytter-matcher (2026-05-30, 88% af 8.699 ryttere usynlige for matching, `.claude/learnings/2026-05-30-pcm-matcher-1000-row-pagination.md`) og sponsor race-day-bonus (#3315, 2026-08-04, ét menneskehold underbetalt 94%, `.claude/learnings/2026-08-04-sponsor-race-results-unpaginated-query.md`). Verificerede prod-rækketal pr. 2026-08-05-audittet (#3331): `race_results` op til **16.994 rækker pr. løb** (213 løb allerede over 1000!) og op til 5.130 pr. hold; `race_entries` maks 192 pr. løb / 546 pr. hold; `race_stage_schedule`/`race_stage_profiles` maks 21 pr. løb; `riders` maks 38 pr. hold; `finance_transactions` maks 305 pr. hold (voksende — se `notifications` maks 920 pr. bruger, tæt på loftet). Deny-listede tabeller (mindst): `race_results`, `riders`, `race_entries`, `race_stage_schedule`, `race_stage_profiles`, `finance_transactions`, `notifications`.
  - **Fix:** paginér med `fetchAllRows`/`fetchAllRowsChunkedIn` (`backend/lib/supabasePagination.js` + frontend-ækvivalenten `frontend/src/lib/supabasePagination.js`). `buildQuery` SKAL inkludere en stabil `.order()` (typisk `.order("id", { ascending: true })`), ellers kan sider overlappe/springe rækker.
  - **Forward-guard:** `scripts/lint-pagination-guard.mjs` (+ test) kører i `preflight-pr.ps1` og CI (`pagination-guard`-jobbet). Flagger `.from(<deny-listet tabel>).select(...)` uden `.limit()/.single()/.maybeSingle()/.range()` og uden delegering til en `fetchAllRows*`-helper. Break-glass-escape: en `// pagination-safe: <begrundelse>`-kommentar på kald-linjen, linjen over, eller trailing på kæden — kun til queries hvor rækkeloftet BEVISLIGT ikke kan nås (fx `.eq("team_id", x)` mod `riders`, bounded af squad-cap). Baseline-ratchet (`scripts/pagination-guard-baseline.json`, samme mønster som `lint-ui-slop.mjs`) grandfathrer pre-#3331-fund — kun NYE overtrædelser fejler builden.
- **Skadet rytter kan ikke starte, uanset flade** (#4057, låst 21/8): hverken auto-fyld/-udtagelses-kandidatpuljer ELLER en manager-udtaget `race_entries`-række må sende en skadet rytter i startfeltet. Kanonisk predikat `isRiderInjured(injuredUntil, todayStr)` + SQL-siden `applyInjuredFilter` + `filterOutInjuredEntries` i `backend/lib/riderEligibility.js` — én definition i stedet for de 5 inline-varianter der stod før. `loadEntrantsForRace` (`backend/lib/raceRunner.js`) filtrerer nu OGSÅ committede manager-udtagne entries, ikke kun de auto-genererede kandidatpuljer (rod-årsagen til #3896: en rask-udtaget rytter der blev skadet før løbsstart kunne stå i feltet og score). For etape 2+ er feltet allerede frosset til etape-1-snapshottet (#1844), så eksklusionen rammer kun feltet før løbsstart.
- **Occupancy-tælleregel for entry-puljer** (#4199, låst 24/8): en plads i en entry-pulje er optaget når holdet hverken er banken (`is_bank`) eller allerede markeret til fjernelse (`pending_removal_at`) — GÆLDER OGSÅ et FROSSET hold, som stadig står i puljen/kalenderen/løbene. `pickDivisionForNewTeam` (`backend/lib/teamProfileEngine.js`) beregner nu to separate tal: `occupancy` (mætning + valg af mindst-fyldte entry-pulje — samme definition som #2377-invarianten i `audit-league-size-invariant.js`) og `realManagers` (KUN til jævn spredning af spillere i overflow-divisionen D4, hvor AI-fyldet altid holder puljen på 24). Før #4183 brugte mætningstesten `realManagers` alene, så et frosset holds plads var usynlig for placeringen af nye tilmeldinger — målt i prod 24/8: to D3-puljer med ét frosset hold hver læste 24 i stedet for 25 og fik derfor begge dagens nye signups oveni.
- **Empty-pool-undtagelsen** (#4182, låst 24/8): et løb i en entry-pulje uden ét eneste hold blokerer hverken season-end-gaten eller transition-gatens `all_races_completed`-tjek, men BEVARES i kalenderen — puljen kan aktiveres igen af en ny tilmelding. Fælles diskriminator `inEmptyPool` i `backend/lib/emptyPoolPolicy.js` (tidligere duplikeret i `stageScheduler.js` + `seasonTransitionReadiness.js`, og manglede helt i `all_races_completed`, som derfor kunne blokere en transition selvom season-end-gaten korrekt så bort fra samme løb).
- **Pulje-fordeling ved pyramide-komprimering** (#4172/#4185, låst 24/8): `distributeCompression` (`backend/lib/pyramidCompression.js`) fordeler tier 4-hold over `d4PoolCount` tier 4-puljer — defaulten er `null` = ALLE eksisterende D4-puljer. Før #4172 var defaulten hardkodet til 2, hvilket ved S1→S2-komprimeringen lod alle 48 D4-hold lande i kun pulje A+B, mens C-H stod tomme med 156 uafviklelige løb i kalenderen. Et eksplicit tal skærer stadig fra toppen af puljelisten (kan bruges til bevidst at reproducere en historisk fordeling).
- **Sæsonskiftets status-kontrakt** (#4228, låst 25/8): `backend/scripts/dev/seasonRollover.mjs` ejer selv nedetids-vinduet. Kommer sæsonen ind som `active`, sætter scriptet den til `upcoming` under den destruktive ombygning og TILBAGE til `active` bagefter — i et `try/finally`, så tilbage-sætningen også sker hvis et trin undervejs fejler. Kommer sæsonen ind som `upcoming` (operatørens egen tilstand ved indgangen), efterlades den `upcoming` — scriptet tænder ALDRIG en sæson der ikke var tændt i forvejen (at gen-tænde et live system er ejer-only, se `.claude/learnings`/memory om samme regel). Forward-guard: `seasonRolloverRestoresActive.test.js` (verificeret rød mod den gamle udgave, grøn mod den nye). Udløst af 25/8-hændelsen (#4229): sæson 3 stod `upcoming` med 0 løb kørt i ca. 4 timer, fordi et menneske var systemets eneste "finally-blok".

## Ved ændring

Hvis nogen invariant skal ændres: opdatér både her, relevant kode-fil, og `docs/decisions/` ADR hvis det er en strategisk beslutning (fx sponsor-niveau).
