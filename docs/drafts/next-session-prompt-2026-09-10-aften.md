# Prompt til aftensessionen 10/9 (skrevet 10/9 kl. 19:25)

Model + indsats: hovedtråd Fable, high. Workers: opus til kode/review, sonnet til docs. `model` EKSPLICIT i hvert Agent-/Workflow-kald. Kopiér teksten under stregen ind som første besked i en ny Claude Code-session i C:\Dev\CyclingZone.

Sådan kørte eftermiddagen (gentag): design-kort med ÉT samlet før/efter-billede (ejeren afviste fire løse mockups); prod-skridt med tal i selve kortet og ordret "kør"; go-kort bygget på `gh pr diff` + reviewerens målte forbehold; workers med reviewer + ret-trin; små opfølgninger som Agent-kald i SAMME worktree (ingen nye baner). Faldgruber: (1) CI tavs på en PR = merge-konflikt mod main (`gh pr view N --json mergeStateStatus` = DIRTY), ikke Actions-fejl; kostede 20 min. (2) Efter hvert docs-merge til main: `git merge-tree --write-tree origin/main origin/<branch> | grep CONFLICT` på alle åbne baner. (3) `Get-Date` FØR hver logning; jeg estimerede 10 min forkert. (4) `jq` kræver fil eller pipe (hook blokerer variabel-sti); brug `cat fil | jq`. (5) Reparations-scripts kræver `infisical run --env=prod --`, ikke dotenv.

---

Ny session (aften 10/9). Læs docs/NOW.md først, derefter dette. Ejeren er ved maskinen og vil "tale om dette og afslutte det". Alt der skriver i prod venter på ordret "merge"/"kør".

Tilstand ved start: MERGET i dag: #5097, #5100, #5071, #5110, #5090 (GDD D-001 til D-048 på main), #5109 (pensionsvarsel gemt; reparation KØRT kl. 19:2x, 7.261 rækker, #5073 lukket), #5111 (D-047 mobiltabeller live kl. 19:07, patch note 7.269, Sentry 0 fejl på releasen). Prod-flag: `rider_reputation_enabled` = shadow (siden 15:45; 7-dages audit ved hver session, #1099). ÅBENT: PR #5108 (#4983 påmindelse) er klar, CI grøn på nær kendt audit, MERGES IKKE før samtalen.

Fase 0 (Fable, read-only, 5 min): `Get-Date` · omdømme-audit: SQL `select count(*) from rider_reputation_events where created_at > '2026-09-10T13:46Z'` + fordeling (p50/≥70/≥90) → kommentar på #1099 · #5111 post-verify på prod-mobil (ejeren kigger selv; spørg om han har set Mit hold på telefonen) · `gh pr view 5108 --json mergeStateStatus` (skal være ikke-DIRTY; ellers merge main ind i worktreet C:\Dev\CyclingZone-worktrees\feat-4983-selection-deadline-reminder).

Fase 1 (samtalen, ÉT kort ad gangen, kontekst INDE i kortet): #4983/#5108 + late_fill. Fakta: prod-måling 17:50 viste 140 (hold,løb)-par / 108 managere med HELT TOMME trupper inden for 24 t; de er røde under både gammel og ny regel, fordi assistenten i dag (proactive) først fylder ved etape 1. Rød-teksten "Holdet stiller ikke op" er falsk for dem indtil `assistant_selection_mode` flippes til `late_fill` (D-034; mekanik bygget, migreret 3/9 #4201, sweep hver time i backend/cron.js). Anbefaling: (1) flip late_fill (SQL `update app_config set value='"late_fill"'::jsonb where key='assistant_selection_mode' and value='"proactive"'::jsonb`), post-verify at næste entry-generator-sweep fylder tomme trupper 24 t før start (tæl race_entries for de 140 par), (2) merge #5108 via `scripts/merge-queue.ps1 -Pr "5108"`, (3) post-verify GET /api/me/selection-reminder i prod-log + Sentry, (4) patch note 7.270 (påmindelsen) + Hjælp-afsnit (#5112 har listen), (5) NOW.md. Rækkefølgen kan ejeren vende; spørg med tal.

Fase 2 (hvis tid): spørgeskema-skub (28 gennemført, 9 begyndt, 218 ikke svaret, ingen lukkedato): skriv indbakke- + Discord-tekst (EN først, DA under, docs/TONE_OF_VOICE.md) i docs/drafts, ejeren sender selv; foreslå lukkedato søndag 14/9. Derefter #5112 (docs-oprydning, sonnet-worker) og bane 1: #4845 kalenderpakker → #4270 S4-kalender (ejer A/B).

Ejer-trin der venter: post Discord-udkastet om pensionsvarslet (docs/drafts/discord-5073-retirement-notice-2026-09-10.md) · fog of war-afstemning (#5107) · #4346 anmeld-handel · flyt forumkategori · nøgler (PostHog, GSC, Resend-webhook, EUR-testkøb, `AUTO_MERGE_PAT`, `SUPABASE_ACCESS_TOKEN`) · visuel identitet #5113 (ejeren kører selv Claude Design-prompten).

Regler: TIER WAVE for workers; ét kort ad gangen; go-kort på diffen; UI merges kun efter billeder + ejer-go (ejeren valgte selv at merge #5111 uset, "så ser vi til det når det er live"); workers rører aldrig docs/NOW.md/MASTERPLAN/PatchNotesPage; close-out: NOW.md ≤1.200 tokens (tjek FØR add), Working agent nulstillet, `scripts/close-out-cleanup.ps1`, `scripts/check-agent-token-hygiene.ps1`.
