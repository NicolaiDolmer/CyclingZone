# Prompt til næste session: workflow-session, kort design + eksekvering til kunderne (skrevet 10/9 2026 kl. 15:00)

Model + indsats: hovedtråd Fable, high. Workers: opus til kode/review/rod-årsag, sonnet til docs/issues/research; `model` EKSPLICIT i hver `agent()`/Agent-kald. Kopiér teksten under stregen ind som første besked i en ny Claude Code-session i C:\Dev\CyclingZone.

**Sådan kørte 10/9 (gentag det gode):** research som 3-4 sonnet/opus-workers til FILER i `docs/audits/` FØR nogen beslutning · byggebølge som ÉN Workflow med synlige faser (byg → uafhængig reviewer → ret-trin i samme worktree → go-kort), pipeline pr. bane, reviewer med opus og skarp "prøv at afvise merge"-prompt: fangede 2-3 reelle fund pr. PR · designsamtale i forgrunden mens workers kørte; hver beslutning logget straks (DECISIONS ordret svar, SESSION_LOG, CLAUDE_HANDOFF-tabel, GDD, områdets SSOT-note) og committet bag branch-guard · go-kort bygget på `gh pr diff` + reviewerens målte forbehold, aldrig PR-teksten · issue-runder med dublet-søgning (kommentar på ejer først, nyt issue kun uden ejer).

**Faldgruber 10/9 (undgå dem):** (1) `cd` i et Bash-kald flyttede arbejdsmappen for resten af sessionen: absolutte stier, aldrig `cd`. (2) Klokkeslæt blev gættet: kald `Get-Date` (PowerShell-værktøjet) FØR hver logning; Git Bash `date` og `pwsh -Command` fra Bash er ikke pålidelige. (3) NOW.md/MASTERPLAN-budgettet blokerer commit via hook: kør `scripts/check-agent-token-hygiene.ps1` FØR `git add`, trim først. (4) Merge-køen flytter main mens du arbejder: efter hver merge `git pull --rebase` FØR næste docs-commit; `.claude/launch.json` er lokalt ændret og blokerer rebase: gem diff med `git diff > patch`, `git checkout -- .claude/launch.json`, rebase, `git apply patch` (aldrig bare `git stash`). (5) `merge-queue.ps1` i baggrund skriver intet til output-filen: følg køen med `gh pr view N --json state`. (6) Friske worktrees mangler backend-env (`Missing SUPABASE_URL`): backend-tests kører i CI, ikke lokalt; skriv det i worker-prompten. (7) To reviewere kan modsige hinanden om CI-tilstand (league-check rød/grøn): afgør med `gh run list --workflow` + `gh pr checks --required`, ikke med docs. (8) Ejeren afviste et UI-fix (#5099) fordi det var endnu en lap: UI-baner skal vise 2-3 modeller VISUELT før byg, ikke bygge den nærmeste løsning. (9) Ét kort blandede to områder (evne + synlighed) og blev afvist: ét område pr. kort, muligheder vist visuelt før kortet, "1 + tilføjelse" bevares ordret. (10) En worker fik et forkert filnavn i prompten og en anden gættede issue-nr (#1154/#1148): verificér stier og numre med `ls`/`gh issue view` før du skriver dem i en prompt.

---

Ny session (workflow-session). Læs docs/NOW.md først, derefter dette. Du er arkitekt; workers bygger. Ejeren er ved maskinen. Alt der skriver i prod venter på ejerens ordrette "merge" eller "kør". Ejerens ønske 10/9: "en ny fantastisk session, hvor vi snakker og designer en smule mere, men derefter inde i den session også har stort fokus på at eksekvere på de meget vigtige ting, som vi gerne vil have, at de kommer live nu her til kunderne".

Tilstand ved start (10/9 kl. 15:00): #5097 (chunk-fejlens rod-årsag) og #5100 (rytterkortets kald halveret) er MERGET 10/9 kl. 14:43/14:50 med Deploy verify grøn; post-verify er IKKE gjort. #5099 (mobil sticky) er parkeret som draft; #5102 = ny mobil-tabelstandard (design). #5071 (docs, rebaset) og #5090 (GDD docs-PR, D-001 til D-046, preflight grøn, NOW.md ude af branchen) venter på ejer-go. GDD: `docs/design/gdd/CLAUDE_HANDOFF.md` er indgangen; 18 beslutninger 10/9 (D-029 til D-046); Q-037 parkeret til forum-afstemning (#5107). MASTERPLAN revideret 10/9 (brand-spor, vækst-fundament SEO/hastighed/mobil, U23/junior løftet til nr. 3). Spillerdata: `docs/audits/2026-09-10-*`. Issues fra 10/9: #5088, #5089, #5091–#5096, #5101–#5107.

Fase 0 (10 min, Fable selv, read-only): `Get-Date` · post-verify #5097: Sentry `search_issues` CYCLINGZONE-56 + `search_events` efter deploy-tidspunktet: ingen nye events med "resolved to an invalid module without a default export"; nye chunk-fejl har URL → notér de 3 hyppigste chunk-URL'er i #4595 · post-verify #5100: Railway `get_logs` (http, status 429, since 24h) mod baseline (flere hundrede på 4 ms 10/9 10:12 UTC) → kommentar på #5089 · `gh pr checks 5071 5090` · skriv Working agent i NOW.md.

Fase 1 (Workflow, start FØR designsamtalen, 4 baner, `isolation`/egne worktrees, reviewer + ret-trin pr. bane som 10/9):
1. opus · #4983 påmindelse før udtagelsesfrist (D-034's synlige del): gul markering i navigationen + boks på planlægningssiden + eskalering nær frist + indstilling; læs ASSISTANT_RULES §1b, PLANNING_CENTER_RULES §4, PAGE_TEMPLATES, TASTE; UI = billeder (Playwright 1280 + 375x812) før go-kort. Ingen tilstands-flip (`late_fill` er ejer-gated).
2. opus · #5073 pensionsvarslet skifter midt i sæsonen: rod-årsag READ-ONLY mod prod (riderSeasonAge, referenceår S3=2028, sweep der skriver varslet), bevis i issue-kommentar + forslag til datareparation som `--dry-run`-script; INGEN mutation.
3. sonnet · #4872 rytterværdi står stille (meldt igen 9/9): read-only SQL mod prod, holder fixet fra 6/9? Kommentar med tal; issue kun hvis nyt.
4. sonnet · docs: #5090 GDD-merge-forberedelse: `docs/META_DOCS_INDEX.md`-linje verificeret, AGENTS.md hard rule 30-listen (#5058) rettet i SAMME PR-serie (egen lille PR mod main), ingen NOW.md.
Reviewer-prompt: "prøv at afvise merge", kør lint + node --test i forgrunden, byg `diff_summary_for_owner` på diffen. Go-kort til ejeren først når bane 1-4 er igennem review + ret.

Fase 2 (design, forgrunden, max 4 kort, hvert med visuel fremstilling FØR kortet, ét område pr. kort, log straks + commit bag guard på `codex/game-design-document`):
a) #5102 Tabeller på mobil: tredje, markant bedre standard. Vis 3 modeller side om side (kort pr. rytter med fold; kolonnevælger + "fuld tabel"-tilstand; fast navnekolonne som egen komponent ved siden af scrollbar datablok) mod PAGE_TEMPLATES T2 + TASTE. Ejerens ord 10/9: "standarden for mobil visningen skal stige markant".
b) Løbsomdømme og personale-omdømme (`REPUTATION_RESEARCH.md` §4-5): forslag = dynamisk prestige-tillæg oven på statisk `race_class` (pkt. 4 A); personale som senere lag (pkt. 3 B). D-041/D-045/D-046 er afgjort; genåbn dem ikke.
c) Popularitet → omdømme konkret (D-042's forudsætning): `riders.popularity` afløses af rytter-omdømmet (#1099, flag `rider_reputation_enabled` off, PR 2-4 fortsætter); sponsormål der peger på popularitet skrives om. Flag-flip er ejer-gated.
d) Kun hvis ejeren vil: assistent-tilstandens flip (D-034, `late_fill` 24 t) som prod-skridt med go-kort (app_config + #4201-migration; ASSISTANT_RULES §1b's fem egenskaber).

Fase 3 (eksekvering): go-kort pr. PR (bane 1 med billeder; bane 4 docs; #5071; #5090) → ejeren siger "merge" → `scripts/merge-queue.ps1 -Pr "N"` én ad gangen (aldrig HH:57-HH:03; `git pull --rebase` efter hver merge) → post-verify → patch note v7.268 samlet (kun for spillervendte ændringer: #4983; #5097/#5100 kræver ingen). Derefter bane 1 uændret: #4845 kalenderpakker → #4270 S4-kalender (ejer A/B), #4850-pakken, favorit-win-rate.

Ejer-trin der venter (uændret): post fog of war-afstemningen (#5107: `docs/drafts/forum-poll-fog-of-war-2026-09-10.md` + PNG) · spørgeskema-reminder (luk ved 40 eller 15/9) · #4346 anmeld-handel-knap lovet 27/8 · #4821 flyt forumkategori lovet "senest 10/9" · PostHog-nøgle · GSC-service-konto · Resend-webhook-secret · EUR-testkøb · `AUTO_MERGE_PAT` · `SUPABASE_ACCESS_TOKEN`.

Regler: TIER WAVE for workers, push <10 min + hvert 15. min, `scripts/wave-lane-watch.ps1 -Once` hvert 15. min, tavs worker 45+15 → TaskStop + afløser i samme worktree, CodeRabbit ved `gh pr ready`, workers rører ALDRIG `docs/NOW.md`/MASTERPLAN/PatchNotesPage, ingen patch notes i PR'er (samlet ved close-out), UI merges kun efter billeder + ejer-go, ét ejer-kort ad gangen med kontekst INDE i kortet, svar på hver ejer-besked straks. Copy = docs/TONE_OF_VOICE.md. Close-out: NOW.md ≤1.200 tokens (tjek FØR add), Working agent nulstillet, uafsluttet som issues, ny prompt i docs/drafts/, `scripts/close-out-cleanup.ps1`, `scripts/check-agent-token-hygiene.ps1`.
