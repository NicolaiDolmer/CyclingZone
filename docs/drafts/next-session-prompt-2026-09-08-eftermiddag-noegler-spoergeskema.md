Prompt til næste session: mail-nøgler, spørgeskema-åbning, målinger, kalender (skrevet 8/9 2026 ca. 10:45 ved close-out af morgen-merge-sessionen)
Model + indsats: hovedtråd Fable, high. Workers: opus til UI/konflikter, sonnet til ops/docs. Ét ejer-kort ad gangen, kontekst INDE i kortet, billeder SENDT før kortet, go-kort bygget på `gh pr diff` + billeder du selv har set. Første kommando: `Get-Date` (Git Bash `date` er UTC). Workers spawner ALDRIG egne agenter og kører ALDRIG hele e2e-suiten. Kopiér teksten under stregen ind som første besked i en ny Claude Code-session i `C:\Dev\CyclingZone`.

---
Ny session. Læs `docs/NOW.md` først, derefter dette. Du er arkitekt; workers bygger med `model` eksplicit. Ejeren er ved maskinen. Alt der skriver i prod venter på ejerens ordrette "merge" eller "kør". Ingen nye features før nedenstående er igennem (ejer 7/9: "afslut det igangværende optimalt og hurtigt").

Tilstand ved start: hele forum- og beskedpakken er merget 8/9 formiddag (#5029 #5027 #5028 #5020 #5018 #5026 #5022 #5019 #5021 #5034). Patch note v7.264 + Hjælp-faner Forum/Beskeder er live. Spørgeskemaet ligger som draft i prod (11 spørgsmål). Dag-1-mail + velkomst står på dry_run siden 8/9 kl. 07:52 (race_digest off).

Del 1: mail-nøgler (ejerens opgave, du guider). Ejeren lægger RESEND_API_KEY (Resend-dashboard, domænet cyclingzone.org er verified) og EMAIL_UNSUB_SECRET (lang tilfældig streng) i Infisical prod og synker til Railway. Du verificerer KUN tilstedeværelse via scratch-script der printer SAT/MANGLER (aldrig værdier; hooken blokerer `console.log(process.env)`-mønstre, brug en fil). Derefter: læs `email_log` (runbook `docs/EMAIL_LOOP_GO_LIVE_RUNBOOK.md` §3): kandidater, targeting, fejl. Er der dry_run-rækker og 0 fejl → copy-kort til ejeren (`docs/drafts/mailtekster-2853-v2-dolmer-2026-09-02.md`, kun EN sendes i dag) → "kør" = flip welcome + day1 til on (SQL i runbook §2). Rollback = off. Er dry_run-loggen tom efter 24 t: find ud af hvorfor (sweep-cron, kandidat-filter) før flip. Mål: dag-7/dag-14 for næste kohorte på 15+ mod 30 %/25 % (#4964).

Del 2: spørgeskema (#4943). Ejeren tester som admin på cyclingzone.org/survey/2026-09-features (draft = admin-only via RLS). Rettelser → worker på seedet (idempotent, `ON CONFLICT DO UPDATE`; slet fjernede keys eksplicit) + `lint-sql-insert-arity`. Åbning ("kør"): `UPDATE public.surveys SET status='open', opens_at=NOW(), updated_at=NOW() WHERE slug='2026-09-features';` → `node backend/scripts/sendSurveyInvite.mjs --survey 2026-09-features --dry-run` (fra backend/, infisical) → vis antal + 3 eksempel-modtagere → "kør" → `--apply`. Så Discord-post-udkast (ejeren poster; ingen tal/datoer, glad tone, `docs/TONE_OF_VOICE.md`) + patch note v7.265 + dashboard-kortet bliver synligt automatisk (SurveyInviteCard). Analyse-SQL i `docs/SURVEY_SYSTEM.md` §5; NPS joines fra nps_responses.

Del 3: målinger (læs, ikke byg). a) #4595 chunk: `infisical run --env=dev -- node scripts/sentry-issues.mjs --period=24h` → CYCLINGZONE-56 events pr. deploy siden 8/9 kl. 10:30 (#5021 merget) mod baseline 3,30/2,60; tjek at seneste docs-only-deploy gav identiske asset-navne. Falder det ikke markant → byg lag 3 (#5033, opus, halv dag) efter ejer-go. b) NPS #4997 (`users.nps_last_prompted_at`, `nps_responses`, `player_events nps_*`) + roadmap-stemmer på de 4 nye punkter (genmål 9/9 aftalt). c) Discord: bekræft at Results D2/D3/D4-samlekanalerne har været stille siden 7/9 kl. 21:45 (#4999).

Del 4: ejer-valg der venter, ét kort ad gangen når Del 1-3 er igennem: #4270 S4-kalender (A: byg #4845 først, apply 14-18/9 / B: apply nu med gammel pakker; hård grænse 27/9; ejeren udskød 7/9 og 8/9) · #4860 sponsor S4-tilbud (før 27/9) · #4616 EUR-nøgler (30 min) · DM-opfølgning #5032 (CASCADE ved kontosletning vs. aldrig-slet-reglen) · #4753 league-size-audit rød på alle PR'er siden 5/9 (D4-F 25 hold; trim eller flyt auditten til dagligt job) · #3069 feature-liveness drift (gammel migration, menneske-beslutning) · vagter foreslået 7/9: migrationer mod tom Postgres i CI, tone-lint (forbudte termer + æøå), worker-skabelon i docs/briefs (regler: ingen fuld e2e, ingen egne agenter, afslut synkront, rebase før screenshots, guard-sti `scripts/guard-commit-branch.sh`).

Regler: TIER WAVE for workers (målrettede tests; ALDRIG hele e2e-suiten fra en worker), push inden 10 min og hvert 15. min, `scripts/wave-lane-watch.ps1 -Once` hvert 15. min, tavs worker 45+15 min → TaskStop + afløser i samme worktree (SendMessage findes ikke), TaskStop-fejlbeskeden lister ALLE kørende agenter (tjek for efterladte under-agenter ved close-out), merges via `scripts/merge-queue.ps1` (én ad gangen; efter hver `database/*.sql`: tjek auto-migrate-run + post-verify FØR næste; ved flere PR'er i samme filer: merge i rækkefølge og lad næste flette main før dens tur), CodeRabbit CLI før `gh pr ready` (`%LOCALAPPDATA%\Programs\coderabbit\coderabbit.exe review --base main --committed`, fra PowerShell i worktreet; CLI'en kan selv committe og pushe), ingen patch notes i PR'er (samlet), UI merges kun efter billeder + ejer-go, subagenter har ikke Discord-MCP. Copy = `docs/TONE_OF_VOICE.md` (ingen tankestreger, æøå i dansk, aldrig "Founder Supporter"). Svar på hver ejer-besked straks; vent aldrig blokerende. Close-out: NOW.md ≤1.200 tokens (tegn/4), Working agent nulstillet, uafsluttet som issues, ny prompt i `docs/drafts/`, `scripts/close-out-cleanup.ps1` (kun når ingen worker kører).
