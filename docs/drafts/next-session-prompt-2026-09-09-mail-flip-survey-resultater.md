# Prompt til næste session: mail-flip, spørgeskema-resultater, mail-drift (skrevet 8/9 2026 sidst på eftermiddagen)

Model + indsats: hovedtråd Fable, high. Workers: opus til UI/backend, sonnet til docs/ops. Ét ejer-kort ad gangen, kontekst INDE i kortet, billeder SENDT før kortet, go-kort bygget på `gh pr diff` + billeder du selv har set. Første kommando: `Get-Date` (Git Bash `date` er UTC). Workers spawner ALDRIG egne agenter, kører ALDRIG hele e2e-suiten og kører preflight/tests i FORGRUNDEN (en worker der "venter på baggrundsjob" er stoppet: tjek worktreet og afslut selv). Kopiér teksten under stregen ind som første besked i en ny Claude Code-session i C:\Dev\CyclingZone.

---

Ny session. Læs docs/NOW.md først, derefter dette. Du er arkitekt; workers bygger med model eksplicit. Ejeren er ved maskinen. Alt der skriver i prod venter på ejerens ordrette "merge" eller "kør". Ejer-rækkefølge 8/9: spørgeskema-flowet skal være helt optimalt FØR mail afsluttes.

Tilstand ved start (8/9 aften): spørgeskemaet `2026-09-features` er ÅBENT siden 8/9 kl. 14:15, 241 inviteret via indbakken, Discord-opslag postet af ejeren. Tre prod-hændelser samme dag, alle lukket: manglende tabel-grants (gem), manglende UPDATE-grant (Send/upsert), mellemrum trimmet i fritekst. Merget 8/9: #5035 (admin-preview + v3-indhold), #5036 (mailskabelon), #5037 (patch note v7.265), #5039 (5 minutter), #5040 (kortet øverst på dashboardet), #5041 (mellemrum), #5042 (grants-migration + lint "policy uden grant" + postmortem), #5044 (sticky fremdriftslinje). 4 spillere blev backfyldt som gennemført (Send-fejlen). Åbne draft-PR'er: #5038 mail-drift-pakken (grøn), #5045 gennemsigtig wordmark (grøn), admin-resultatside (opus-worker, tjek `gh pr list --search "survey results"`). Mail: nøgler i Infisical prod + Railway, dry_run virker, testmail leveret, **welcome/day1 stadig dry_run** (flip ikke sket).

Del 1: spørgeskema-resultater. a) Admin-resultatsiden `/admin/surveys/2026-09-features`: hvis PR'en er draft, læs diffen, se screenshots, go-kort, merge, post-verify på prod som admin. b) Tal-status i kortet: startede, gennemførte, svarprocent af 241, fog of war-fordeling, top-5 idéer efter idé × vigtigt (SQL i docs/SURVEY_SYSTEM.md §5). c) Frafald pr. spørgsmål (0 frafald blandt de første 7). d) Følg op på Discord-tråden om skemaet (ejeren paster; du svarer aldrig selv).

Del 2: mail (når ejeren siger til). a) #5045 merge → ny testmail til hotmail fra main (`scratch send-test-welcome.mjs` ligger i forrige sessions scratchpad; skriv den igen efter mønsteret: importér backend/lib/emailTemplates.js + emailService.js, `infisical run --env=prod`, unik idempotency-key, aldrig email_log) → ejer ser Outlook dark mode. b) #5038 mail-drift: go-kort (26 filer, 2 migrationer: email_events, email_sweep_runs), merge, post-verify-SQL fra PR-body, derefter ejer-trin: Resend-webhook (URL + events i PR-body) + RESEND_WEBHOOK_SECRET i Infisical/Railway, valgfrit Receiving/MX + EMAIL_REPLY_FORWARD_TO, Postmaster Tools + SNDS, DMARC-plan i runbook §6. c) Flip: SQL i runbook §2. OBS: efter #5038 blokerer dry_run-rækker ikke længere, så DELETE af dry_run-rækken er unødvendig; før #5038 skal `welcome:b9e5fdb9-150c-4f3b-bbaa-a477ce9fb5f0` slettes først. Vinduet for den første rigtige kandidat lukker 10/9 kl. 12:14. Kohorte-mål #4964: dag-7/dag-14 30 %/25 %.

Del 3: målinger (læs). #4595 chunk: `infisical run --env=dev -- node scripts/sentry-issues.mjs --period=24h`; entry-chunken roterer stadig pr. deploy (Sentry-plugin debug-ID, se kommentar 8/9 på #4595); ejer-valg: rod-årsag (post-build sentry-cli inject + guard med dummy-Sentry-env) FØR #5033 lag 3. NPS #4997 + roadmap-stemmer på de 4 nye punkter (genmål 9/9). Sentry: 5P/5N kan markeres løst.

Del 4: ejer-valg der venter, ét kort ad gangen: #4270 S4-kalender (A/B, grænse 27/9) · #4860 sponsor S4 · #4616 EUR-nøgler · #5032 DM-opfølgning · #4753 league-size-audit rød · #3069 feature-liveness · 16 ældre "policy uden grant"-fund fra lint'et (ingen skrives fra frontend; hygiejne, listet i #5042) · `bg-cz-bg` udefineret token 5 steder i admin (listet i #5044).

Regler: TIER WAVE for workers, push <10 min + hvert 15. min, `scripts/wave-lane-watch.ps1 -Once` hvert 15. min, tavs worker 45+15 → TaskStop + afløser i samme worktree, merges via `scripts/merge-queue.ps1` (én ad gangen; efter database/*.sql: tjek auto-migrate-run + post-verify før næste; vent til checks har været rene i to aflæsninger før køen startes), CodeRabbit CLI før `gh pr ready`, ingen patch notes i PR'er (samlet), UI merges kun efter billeder + ejer-go, subagenter har ikke Discord-MCP. Copy = docs/TONE_OF_VOICE.md. Svar på hver ejer-besked straks. Close-out: NOW.md ≤1.200 tokens, Working agent nulstillet, uafsluttet som issues, ny prompt i docs/drafts/, `scripts/close-out-cleanup.ps1`.
