# NOW — Aktuel arbejdsstatus

## Aktiv slice
**Polish-sprint frem til open beta (#178)** — mobile 360px touch-target-blockers ryddet via #181. Næste fokus: onboarding friction-audit + Supabase-key-rotation + verifikations-kø.

## Senest leveret
*(Fuld historik før denne komprimering er arkiveret i [`docs/archive/NOW_HISTORIK_2026-05-08-DX-PRECOMPACT.md`](archive/NOW_HISTORIK_2026-05-08-DX-PRECOMPACT.md).)*

- 2026-05-09: **07h Sæson-finansrapport per hold LIVE** (v2.97, [#86](https://github.com/NicolaiDolmer/CyclingZone/issues/86)) — ny privat finansrapport per sæson per hold på `/seasons/:seasonId/finance/:teamId`. Hero-kort viser totalt indtægt/udgift/net cashflow, to recharts donut-diagrammer fordeler indtægter og udgifter på reason_code (sponsor, præmiepenge, auktion-køb, løn osv.), top-3-lister fremhæver de største transaktioner i hver retning, og loan-portfolio-tabellen viser aktive lån med restgæld + estimeret næste-sæson-rente. Auth-gate på endpoint: kun team-owner ELLER admin. Sponsor-modifier-kurve placeholder vist indtil sæson 2 (board_plan_snapshots tom). Migration `2026-05-09-season-zero-finance-tx-backfill.sql` seedede sæson 0 (open beta transfervindue, 2026-05-08T18:00Z), backfill'ede 82 finance_transactions med season_id + reason_code (heuristisk via type+description, alle var auktion-rows), og tilføjede BEFORE INSERT-trigger der auto-stamper season_id på fremtidige rows — zero callsite-changes nødvendige. Pure-function `buildSeasonFinanceReport` (15 unit-tests inkl. privacy-test som verificerer audit-internals ikke lækker). Entry-points: 📊 Sæsonsrapport-knap på FinancePage + 📊 Finansrapport-knap på SeasonEndPage. 463/463 backend-tests grønne (+15 nye). CI + Deploy verify + CodeQL + Auto-migrate alle `success` på merge-SHA 68d4c9d.
- 2026-05-09: **07g Manager finance-forecast + risk-tier LIVE** (v2.96, [#85](https://github.com/NicolaiDolmer/CyclingZone/issues/85)) — ny pure-function `computeFinanceForecast` projicerer næste sæsons cashflow (sponsor × board-modifier × pullout-faktor + sum(prize_earnings_bonus) − sum(salary) − sum(amount_remaining × interest_rate) − lejegebyr-net) og returnerer 🟢/🟡/🔴 risk-tier + actionable warnings. Tærskler: grøn = net ≥ +50K og gæld < 50% af loftet, gul = net ∈ [±50K] eller gæld 50-80%, rød = net < -50K eller gæld > 80% eller debt-trend rammer ceiling inden for 2 sæsoner. UI: dedikeret prognose-sektion på FinancePage (breakdown + ±20% spænd + warnings), `FinanceForecastBadge` på Dashboard under squad-warning (synlig altid), HelpPage FAQ. Endpoint `GET /api/me/finance-forecast` bag requireAuth. 448/448 backend-tests grønne (+11 nye for 4 manager-arketyper + 7 edge cases). CI + Deploy verify + CodeQL `success` på merge-SHA bf4a244.
- 2026-05-09: **07e Fase B — admin-handlinger feed + cron-run-korrelering LIVE** (v2.94, [#83](https://github.com/NicolaiDolmer/CyclingZone/issues/83)) — to nye sub-tabs på Økonomi-sektionen: "Admin-handlinger" (paginated admin_log-feed med filter på 24 godkendte action_types + admin user + target hold/rytter + dato-range; klik-row → modal med pretty-printed meta-JSON) og "Korrelering" (finance_transactions grupperet per actor_id+source_path med ±5s tidsvindue → cron-runs nyeste først med tx-count, Σ-beløb, hold ramt, reason-codes; klik-row → drill-down til Transaktioner-view med pre-fyldte filtre). To nye admin-endpoints bag requireAdmin: `GET /api/admin/admin-log` (paginated + filtreret) og `GET /api/admin/cron-runs` (default 7-dages vindue, hard-cap 20K rows, konfigurerbart window_seconds). Pure helper `groupCronRuns` i backend/lib/cronRunCorrelation.js (12 nye unit-tests). 437/437 backend-tests grønne (+16 nye). CSV-bulk-export droppet fra scope — bruger har ikke aktivt behov, ad hoc-eksport kan køres direkte i Supabase Studio. SOAK-GATE bemærkning: post-cutoff (17:00) populated_count=0 fordi der ikke har været finance-aktivitet siden deploy; en enkelt populated row fra 15:00 indikerer Fase B virker, men gaten kan ikke "bevise" sig endnu før der kommer trafik på prod.
- 2026-05-09: **07e Fase A — admin økonomi-dashboard LIVE** (v2.93, [#83](https://github.com/NicolaiDolmer/CyclingZone/issues/83)) — ny Økonomi-sektion på AdminPage med tre sub-views: Sundhed (audit-population NULL-counter + balance-drift watchdog som live health-widgets), Overblik (per-hold tabel: balance, sponsor, gæld, gældsloft, ratio, 🟢/🟡/🔴 sustainability-badge filtreret per division/q/inkl. AI/frosset), Transaktioner (paginated finance_transactions med filter på actor_type/reason_code/type/team/season/source_path/dato/beløb + drill-down-modal med alle 9 audit-kolonner inkl. before/after-balance-invariant-check). Tre nye admin-endpoints (`/economy-overview`, `/finance-transactions`, `/economy-health`) bag requireAdmin med limit-clamping (max 200 rows) — ny `economyAdminDashboard.js` lib med `computeSustainabilityTier` + `computeDebtRatio` pure helpers. 423/423 backend-tests grønne (+8 nye). Soak-gates pre-deploy verificeret: post_phase_b_null=0, balance-drift max=0 på 22 hold. Fase B (admin_log-feed + cron-run-korrelering + CSV-export) parkeret til næste session.
- 2026-05-09: **07d Fase B — audit-population på alle penge-paths LIVE** (v2.92, [#235](https://github.com/NicolaiDolmer/CyclingZone/issues/235)) — alle 26 callsites til `increment_balance_with_audit`-RPC sender nu actor_type/actor_id/source_path/reason_code/related_entity_type/_id i payload; cron-paths (sponsor, salary, divisionsbonus, lejegebyr-paid/received, præmiepenge) får UNIQUE-håndhævet idempotency_key oven på de eksisterende partial UNIQUE-indices fra 07b. Soak-gate verificeret før udrulning: 22 hold, max_drift = 0 mellem teams.balance og SUM(finance_transactions.amount). 5 nye reason-codes (`auction_guaranteed_bank_sale`, `squad_auto_purchase/_sale`, `squad_violation_fine`, `board_bonus_accepted`) tilføjet til FINANCE_REASON. Tests: per-callsite audit-coverage assertions (ny i auditTrail.test.js), eksisterende 7 financeRows-deepEqual-tests opdateret. 415/415 backend-tests grønne (+5 nye). Fundament for 07e admin økonomi-dashboard ([#83](https://github.com/NicolaiDolmer/CyclingZone/issues/83)). Soak-gate efter Fase B: ~1 uge før 07e startes — `SELECT COUNT(*) WHERE actor_type IS NULL` på finance_transactions skal aftage til 0 for nye rows.
- 2026-05-09: **07c Atomic balance-RPC LIVE** (v2.91, [#81](https://github.com/NicolaiDolmer/CyclingZone/issues/81)) — migration `database/2026-05-09-balance-rpc.sql` anvendt direkte på prod via Supabase MCP (`increment_balance_with_audit(team_id, delta, payload jsonb)` returner ny balance, bruger `pg_advisory_xact_lock(team_id)` til at serialisere concurrent calls). Alle ~22 backend-callsites refaktoreret til at gå via RPC: loanEngine (5), economyEngine (creditTeam/debitTeam wrappere → 4 logiske callsites), auctionFinalization (3), transferExecution (6), prizePayoutEngine (1), squadEnforcement (3), api.js (5: loan accept/buyout, admin loan cancel, admin balance, board bonus). 8 nye `balanceAtomicity.test.js` race-tests (10 parallelle deltas → final balance = baseline + Σ deltas, idempotency_key dedup). Live race-test mod test-seller verificerede audit-invariant `after_balance = before_balance + amount` på alle 10 rows. Lost-update-races og partial-write-state mellem balance-mutation + finance_transactions INSERT er nu fysisk umulige. **Blokerer ikke 07d Fase B længere** ([#235](https://github.com/NicolaiDolmer/CyclingZone/issues/235)). 410/410 backend-tests grønne, frontend build OK. Soak-gate 24t før Fase B startes.
- 2026-05-09: **07d Fase A — audit-log foundation LIVE** (v2.90, [#82](https://github.com/NicolaiDolmer/CyclingZone/issues/82)) — migration `database/2026-05-09-audit-log-foundation.sql` anvendt direkte på prod via Supabase MCP (additivt only: 4 nye indices på admin_log + CHECK constraint på 24 godkendte action_types + 9 nullable audit-kolonner på finance_transactions + 4 indices). Fund undervejs: admin_log-tabellen eksisterede ALLEREDE på prod siden 2026-04-29 med 18 rows og 4 action_types — master-planen var forældet, CREATE TABLE blev sprunget over. Faktisk callsite-tal: 11 (ikke 6). Nye `ADMIN_ACTION_TYPE`/`FINANCE_ACTOR_TYPE`/`FINANCE_RELATED_ENTITY`/`FINANCE_REASON` enums i economyConstants.js erstatter hardkodede strings i 11 admin-routes (auctionCancellation + 10 routes i api.js). auctionCancellation skriver nu admin_log med højlydt fejl i stedet for best-effort try/catch. 7 nye tests i `auditTrail.test.js` håndhæver enum⇄CHECK-constraint match. 402/402 backend-tests grønne. Frontend build OK. Fase B (RPC + finance audit-population) afventer 07c. Master-plan + #82 close-out kommentar opdateret.
- 2026-05-09: **Auktion-start gate på pending_team_id LIVE** (commit [814b5dc](https://github.com/NicolaiDolmer/CyclingZone/commit/814b5dc)) — i sæson 0 (ingen åbent transfervindue) sætter `auctionFinalization` `pending_team_id` på winneren i stedet for at flytte `team_id`, så rytteren står som "indgående" hos vinderen. Bug: andre managere kunne starte en helt ny auktion på rytteren via rytter-profil (free agent eller bank/AI-rytter passerede `canAuction`-tjekket; backend POST /api/auctions tjekkede kun `team_id`, ikke `pending_team_id`). Den nye auktion blev senere annulleret af stale-owner-tjekket i finalize, men UX'en var ødelagt — bud bundet, leder fik "auktion annulleret"-besked. Prod-data verificeret: 59 ryttere har pending_team_id netop nu (58 free agents, 1 bank), 4 dobbelt-auktion-instanser fra i dag (alle i `cancelled`-state). Fix: ny pure-funktion `getAuctionStartIssue({ rider })` i auctionRules.js + 2 unit-tests; backend POST /api/auctions returnerer 409 "Rytteren er vundet på en auktion og afventer overførsel"; frontend RiderStatsPage skjuler Start auktion + transfer/swap/loan-knapper og viser 🔒-besked når `isPendingTransfer`. v2.89 patch notes. 395/395 backend-tests grønne (+2 nye). CI + Deploy verify + CodeQL alle `success` på merge-SHA (run [25600990607](https://github.com/NicolaiDolmer/CyclingZone/actions/runs/25600990607)).
- 2026-05-09: **Marked-pause kill switch LIVE** (commit [21f927d](https://github.com/NicolaiDolmer/CyclingZone/commit/21f927d)) — admin kan nu fryse markedet i nødstilfælde via to niveauer i `auction_timing_config`: `auctions` (blokerer POST /auctions, /bid, /proxy) eller `all` (samme + transfertilbud, byttehandler, lejeaftaler, bank-lån). `POST /api/admin/market/pause` med `level` + valgfri `reason`; `POST /api/admin/market/resume` skubber `calculated_end` frem på alle aktive/extended auktioner med pause-varigheden, så bydere får samme resterende tid som da pausen blev slået til. Cron pauser `finalizeExpiredAuctions` mens paused. Cleanup-handlinger (annullér, afvis, træk-tilbage) virker stadig. Ny `backend/lib/marketPause.js` (12 tests). AdminPage får ny "Marked-pause"-sektion + rød banner når paused. v2.88 patch notes. 393/393 backend-tests grønne. Vercel + Railway prod-deploy `success`.
- 2026-05-09: **Auktion-forlængelse +1h grace + døgn-rollover LIVE** — `checkBidExtension` i `auctionEngine.js` hard cap'per nu ved `windowClose + extension_grace_minutes` (default 60) i stedet for præcist windowClose; bud der ville skubbe slutningen forbi det cap, ruller resterende minutter over til næste vindues åbning. Eksempel: fredag bud 22:55 → +10 = 23:05 → 5 min overflow → auktionen slutter lørdag 08:05. 10 nye unit-tests i `backend/lib/auctionEngine.test.js` dækker grace-zonen + hverdag/weekend/weekend→hverdag rollover. Fixede sideløbende time-bomb i `proxyBidding.test.js` (hardkodet `SAT_NOON_UTC` 2026-05-09 var rullet ind i fortiden og fik resolver til at bail på `isAuctionExpired`); bumpet til 2030. HelpPage-tekst opdateret. 382/382 backend-tests grønne. v2.87 patch notes.
- 2026-05-09: **#156 Lejeaftale-cancel kontraktintegritet LIVE** — `PATCH /api/loans/:id action=cancel` blokerer nu på `status=active` (returnerer 400 "kan ikke annulleres ensidigt — kontakt en admin"); pending-flow (borrower trækker eget forslag, lender afviser) bevaret. Ny `getLoanCancelIssue` helper i `transferExecution.js` parallel til swap/transfer-cancel-pattern. Nyt admin-endpoint `POST /api/admin/loans/:id/cancel` (requireAdmin) refunderer automatisk betalt loan_fee (balance + finance_transactions begge veje), logger til `admin_log` med `loan_agreement_admin_cancel` action_type + `prior_status` + `refunded_fee` + `reason` i meta, og notificerer begge parter. TransfersPage skjuler "Annuller lejeaftale"-knappen på active loans og viser i stedet "Aktive lejeaftaler er bindende og kan kun annulleres af en admin." 372/372 backend-tests grønne (+1 ny `getLoanCancelIssue`-test). v2.85 patch notes.
- 2026-05-09: **#158 + #159 + #160 + #198 Transfer-polish LIVE** — Byttehandel + lejeaftale-knapper på rytter-profil (#158); ubegrænset modbud-loop for byttehandel (#159, backend counter-counter + frontend knap); lejeaftale max 1 sæson (#160, backend + frontend); stale 10%-kommentarer fjernet fra auctionRules.js + SQL-migration (#198). v2.84 patch notes.
- 2026-05-09: **#163 RiderStatsPage 360px polish LIVE** ([PR #221](https://github.com/NicolaiDolmer/CyclingZone/pull/221), commit [76b04de](https://github.com/NicolaiDolmer/CyclingZone/commit/76b04de)) — samlet stilling med #181's SquadTab-spørgsmål: stats-tabeller med 14 kolonner forbliver horisontal scroll (FM-style accepteret); fokus på #163 blev RiderStatsPage-detail polish. 7 entydige best-practice fixes: StatRow + Potentiale-label `w-36` → `w-28 sm:w-36` (mere bar-plads); Sæsonhistorik + Løbsresultater wrap i `overflow-x-auto`; Flash auktion-label stack på mobil (overflow-fix); 3 inputs (tilbudsbeløb, besked, auktions-startpris) `text-base sm:text-sm` for at forhindre iOS Safari zoom-on-focus; 3 action-buttons (Send transfertilbud, Send tilbud, Start auktion) `min-h-[44px]` (Apple HIG). Tab `flex-wrap` → `overflow-x-auto` sprunget over (scroll skjuler primary-nav bag højre kant). v2.83 patch notes. CI grøn (backend-tests + frontend-build + Auto PR review + dependency-review). Vercel + Railway prod-deploy `success` på merge-SHA.
- 2026-05-09: **#181 Mobile 360px launch-blockers LIVE** ([PR #220](https://github.com/NicolaiDolmer/CyclingZone/pull/220), commit [674ddd6](https://github.com/NicolaiDolmer/CyclingZone/commit/674ddd6)) — 360px-audit af AuctionsPage + TeamPage + BoardPage gennemført via 3 parallelle Explore-agenter; 11 fund verificeret mod kode (3 false positives elimineret: mobile bruger AuctionCard ikke table; mobile bid-input allerede polished; modaler allerede mx-4). 5 launch-blockers fixet i én konsolideret PR: AuctionsFirstBidHint × button (44×44), OverbidToast × button (44×44 + text-base), RiderFilters Chip (hele chip nu klikbar med aria-label, 44px high), StatsToggle popover (`max-w-[calc(100vw-2rem)]` forhindrer overflow), TeamPage Sælg/Auktion-knap (`min-h-[44px]`). Nice-to-have flagget separat for post-launch (BoardPage `py-2`-knapper, TeamPage transfer-toggles, SquadTab table-vs-card debat → cross-ref #163). v2.82 patch notes. Vercel + Railway prod-deploy `success` på merge-SHA (Deploy verify run 25582399529).
- 2026-05-08: **Auktioner overblik-overhaul LIVE** ([PR #219](https://github.com/NicolaiDolmer/CyclingZone/pull/219), commit [9939603](https://github.com/NicolaiDolmer/CyclingZone/commit/9939603)) — Min situation som ny default-tab (3 sektioner: 🟢 Du leder, 🔴 Du er overbudt, 🔵 Du sælger; tomme sektioner skjules). Stats-toggle (popover, persisted i localStorage, default ingen evne-kolonner). Wishlist-stjerne integreret i rytter-cellen (genbruger WatchlistStar). Værdi → Løn på auktionssiden (sortering bytter til salary). Sticky-left rytter-celle (auction-rider-cell CSS-utility). Kolonner omarrangeret: Rytter | Højeste bud | Tid | Alder | Løn | Pot | Sælger | Stats | Byd. Min/max pris-filter (`min_auction_price`/`max_auction_price`) i RiderFilters bag prop. BidConfirmModal vises ved alle bud (auktion, autobud-loft, transferbud) — wired i AuctionsPage + RiderStatsPage + TransfersPage. AuctionHistoryPage: Værdi → Løn i undertekst. v2.81 patch notes. Vercel + Railway prod-deploy `success` på merge-SHA.
- 2026-05-08: **Sprogvask: 'Auto-by' → 'Autobud' overalt LIVE** ([PR #218](https://github.com/NicolaiDolmer/CyclingZone/pull/218), commit [07c7f18](https://github.com/NicolaiDolmer/CyclingZone/commit/07c7f18)) — UI-strings, ARIA-labels, fejlbeskeder, Discord-DM'er, kommentarer, tests, docs og learnings normaliseret til canonical dansk form `autobud` (et autobud, neutrum). 25 filer + omdøbt `2026-05-08-auto-by-opening-bid.md` → `2026-05-08-autobud-opening-bid.md`. Grammatik-fix: `Din autobud` → `Dit autobud` (neutrum). v2.80 patch notes. Tests: backend 371/371, frontend 4/4. Vercel + Railway prod-deploy `success` på merge-SHA (Deploy verify workflow grøn).
- 2026-05-08: **Auktioner autobud + BYD-kolonne LIVE** ([PR #217](https://github.com/NicolaiDolmer/CyclingZone/pull/217), commit [36ef9b8](https://github.com/NicolaiDolmer/CyclingZone/commit/36ef9b8)) — `PATCH /api/auctions/:id/proxy` sætter nu ikke kun max-loft, men placerer også minimumsbuddet med `auction_bids.is_proxy=true`, når manageren ikke allerede fører. Genbruger eksisterende balance/signing/extension-gates fra `POST /bid`. Desktop BYD sticky-cell har solid baggrund (`auction-bid-cell` CSS-utility) i stedet for gennemsigtigt overlap, og `+ Autobud loft` er løftet til badge-styling med accent-baggrund på desktop + mobil. Ny RLS-migration `2026-05-08-auction-proxy-own-read.sql` anvendt på prod (tabellen havde RLS enabled men ingen policy → frontend kunne ikke læse eget loft). Schema-kontrakttest håndhæver `auction_proxy_bids` ikke-public invariant. v2.79 patch notes. Tests: backend 371/371, frontend 4/4. Vercel + Railway prod-deploys begge `success` på merge-SHA. Refs #10 #197.
- 2026-05-08: **#197 Auktioner mobile polish LIVE** ([PR #216](https://github.com/NicolaiDolmer/CyclingZone/pull/216), commit [1f12a46](https://github.com/NicolaiDolmer/CyclingZone/commit/1f12a46)) — vej A (smal & evidensbaseret): AuctionCard mobile bid + alle proxy-knapper løftet til `min-h-[44px]` (Apple HIG); inputs løftet fra `text-xs`/`text-sm` til `text-base` (16px) for at forhindre iOS Safari auto-zoom på fokus; aria-label på Gem/Ændr/Fjern/+/✕ proxy-knapper i AuctionRow + AuctionCard; `role="status"` + aria-label på indlæsnings-spinner. v2.78 patch notes. AuctionRow scroll-fix-claim fra issue-body droppet — sticky right-0 har været live siden v1.86, ingen 360px-audit har dokumenteret faktisk breakdown. Live verifikation på 360px (Chrome MCP) blokkeret af Vercel preview login-wall — afventer brugerens manuelle DevTools-mål på prod.
- 2026-05-08: **#178 Dependabot-hærdning LIVE** ([PR #210](https://github.com/NicolaiDolmer/CyclingZone/pull/210), commit [7473d0e](https://github.com/NicolaiDolmer/CyclingZone/commit/7473d0e)) — advisory-only mode: dependabot-auto-merge.yml kommenterer kun klassifikation + risiko-vurdering; ingen automatisk merge selv ved patch/minor. Bruger tilføjer manuelt `auto-merge` label for at trigge auto-merge.yml. `frontend/vercel.json` ignoreCommand skipper Vercel preview-builds på `dependabot/*` branches (sparer build-minutes + forhindrer kø-stuvning som blokerede main-deploys 2026-05-08). `dependabot.yml` ignorerer nu major-bumps for react-router-dom + @vitejs/plugin-react (ud over tailwind/vite/react/react-dom). PR #141 + #142 lukket samtidig med `@dependabot ignore this major version`. v2.77 patch notes.
- 2026-05-08: **#196 Realtime UX-pakke på /auctions LIVE** ([PR #208](https://github.com/NicolaiDolmer/CyclingZone/pull/208), commit [254fbe9](https://github.com/NicolaiDolmer/CyclingZone/commit/254fbe9)) — fire patterns i samme PR: (1) desktop-sidebar viser live bud-feed kun for auktioner manageren deltager i (manuelt bud ELLER aktiv proxy), (2) pris-celle pulse-flash 1.5s i `cz-accent` ved current_price-ændring, (3) toast "Du er overbudt på X" når `current_bidder_id` skifter FRA mig (aldrig ved selv-bud eller egen proxy auto-eskalering), (4) aggregat-ticker "X nye bud i sidste 30s" i header — 30s rolling vindue, navne/beløb ikke eksponeret. Supabase channel udvidet til at lytte på `auction_bids` INSERT i tillæg til `auctions` UPDATE. Pure-function helpers i `lib/auctionsRealtime.js` med 4 node:test-tests (pulse-trigger, isOverbidEvent edge cases, feed-filter, ticker-prune). Frontend test-setup etableret (`npm test` = `node --test`, samme pattern som backend). v2.76 patch notes. **Bonus-fix ([PR #209](https://github.com/NicolaiDolmer/CyclingZone/pull/209)):** discovered under Chrome MCP-verifikation at `public.auctions` + `public.auction_bids` aldrig var blevet tilføjet til `supabase_realtime` publicationen, så postgres_changes-events tav i prod — også for #195's bud-timeline subscriber. Migration anvendt direkte i prod, idempotent migration-fil i database/. Live ticker verificeret 0 → 3 → reset efter 30s.
- 2026-05-08: **#195 Bud-timeline på rytter-profil LIVE** — Ny `GET /api/riders/:id/bid-timeline` (lib/`riderBidTimeline.js`) returnerer bud-timeline for aktiv/extended auktion ({team_name, amount, bid_time, is_proxy}) og kun final-shape ({final_bid, winner_name, seller_name, completed_at}) for completed. `proxy_max`/`max_amount` håndhæves som assertion-niveau-invariant — runtime-tjek + 4 tests (active shape, completed-only-final, proxy_max-leak-blockade, no-auction empty). `RiderStatsPage.jsx` har ny "Bud-historik"-tab med Autobud-mærkat på proxy-step-ups; Supabase-channel `rider-bids-{auctionId}` lytter på `auction_bids` INSERT for live updates uden refetch. v2.75 patch notes. 367/367 backend-tests grønne (+4 nye).
- 2026-05-08: **#183 + #174 Auctions-tillidssprint LIVE** — silent-failure-mode i proxy-bidding fjernet: `resolveProxyBids` sletter nu stale winner-proxy (max < currentPrice) fra `auction_proxy_bids` så UI ikke viser "Autobud max ..."-badge for udmattet proxy. Team-lookup hærdet med `.maybeSingle()` (RLS race-safety). Frontend `handleSaveProxy` i AuctionRow + AuctionCard render'er nu backend-fejlbeskeder ("Du kan ikke sætte autobud på din egen rytter", "Max-loft skal være mindst...") i stedet for kun "Fejl"-knap. v2.74 patch notes. 363/363 backend-tests grønne (+1 ny test for stale-proxy-delete).
- 2026-05-08: **#44 Balance-gates på alle user-spend-paths LIVE** ([PR #204](https://github.com/NicolaiDolmer/CyclingZone/pull/204), commit [2083521](https://github.com/NicolaiDolmer/CyclingZone/commit/2083521)) — `computeWorstCaseCommitment` gates POST /bid (proxy_max), PATCH /proxy, resolveProxyBids auto-eskalering, repayLoan, PATCH /loans buyout + accept, transfer-offer accept, swap-cash. UI viser raw + tilgængelig + låst-i-bud i AuctionsPage og FinancePage. Cancellation-safety-net i auctionFinalization bevaret som race-fallback. v2.73 patch notes. 359/359 backend-tests grønne (+20 nye).
- 2026-05-08: **#194 Race-confirm-modal LIVE** ([PR #202](https://github.com/NicolaiDolmer/CyclingZone/pull/202), commit [6db90d8](https://github.com/NicolaiDolmer/CyclingZone/commit/6db90d8)) — backend returnerer 409 ved stale `expected_current_price`; ny `RacePriceModal` viser ny pris/min-bud med Annullér + Byd-knapper; v2.72 patch notes; 339/339 backend-tests grønne (+3 nye for `isExpectedPriceStale`).
- 2026-05-08: **DX-hærdning LIVE** ([PR #201](https://github.com/NicolaiDolmer/CyclingZone/pull/201), commit [d361c5c](https://github.com/NicolaiDolmer/CyclingZone/commit/d361c5c)) — branch protection (backend-tests + frontend-build + dependency-review), secret scanning + push protection, hærdet pre-push hook, agent-doctor.ps1, deterministisk claude-triage, bounded claude-review, `.env` fjernet fra Git og erstattet med `.env.example`. **POST-MERGE: roter Supabase keys fra git-historikken.**
- 2026-05-08: **#193 Reserved-balance off-by-one for proxies LIVE** ([PR #200](https://github.com/NicolaiDolmer/CyclingZone/pull/200), commit [20edc43](https://github.com/NicolaiDolmer/CyclingZone/commit/20edc43)) — `computeReservedBalance` bruger nu `MAX(current_price, own_proxy_max)` per auktion. Backend-only, ingen patch notes.
- 2026-05-08: **#192 Auktions-safety-pakke LIVE** ([PR #199](https://github.com/NicolaiDolmer/CyclingZone/pull/199), commit [a747404](https://github.com/NicolaiDolmer/CyclingZone/commit/a747404)) — owner-check på proxy, logging af silent error paths og Discord DM kun ved udmattet proxy.

## Næste session (prioriteret)
1. **Slice 07e Fase B+ soak-gate verifikation** ([#83](https://github.com/NicolaiDolmer/CyclingZone/issues/83)) — Fase A+B LIVE som v2.94. Audit-population soak-gate skal genkøres når der har været prod-finance-aktivitet siden 17:00 cutoff (auktion-finalize, sponsor-payout, transfer m.v.) for at bevise post_phase_b_populated > 0. CSV-bulk-export droppet bevidst fra scope.
3. **Manuel verifikation på prod** — v2.66 + v2.68 + #193 + #194 (race-confirm-modal: 2 Chrome-tabs, byd nær-samtidigt, modal vises med ny pris/min-bud) + **#197 (mobile-polish: åbn /auctions på iPhone eller Chrome DevTools 360px → mål proxy-knapper med inspector → ≥44px; fokuser bud-input på iPhone Safari → ingen zoom-jump)**.
2. **Roter Supabase keys** — service key + anon key blev historisk committed (fjernet i #201, men lå i Git-historikken).
3. **Mobile UX audit:** #163 + #181 lukket; resterende mobile friction tages ad-hoc.
4. **Onboarding session-1 audit** — start som ny manager og noter friction.
5. **Dependabot post-launch:** PR #141 + #142 lukket + ignored 2026-05-08; #114, #127 afventer post-open-beta. Vurder om advisory-only-mode skal blive permanent eller patch/minor auto-merge skal genaktiveres efter launch.

## Kritiske invarianter
- Verificér runtime før claims; runtime > docs.
- Skaler for variabelt manager-tal; ingen hardcoded antal.
- Economy: DEFAULT_BETA_BALANCE=800000, sponsor=240000, SALARY_RATE=0.10, gældsloft D1/D2/D3=1.2M/900K/600K.
- Auction finalization har parallelle paths i `api.js` og `cron.js`; begge skal delegere til `auctionFinalization.js`.
- AI/bank/frozen får aldrig board-state; manager-only.
