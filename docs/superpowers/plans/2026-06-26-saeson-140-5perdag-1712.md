# Sæson-model #1712 — 140 løbsdage / 5 etaper-per-dag — Implementeringsplan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development eller superpowers:executing-plans. Steps bruger checkbox (`- [ ]`).
> **STATUS: UDKAST TIL EJER-GENNEMGANG.** Bygges IKKE før ejeren har bekræftet afviklings-modellen (§1) + svaret på de åbne spørgsmål (§7). Lektion 2026-06-26: byg ikke #1712 ad-hoc — den var planlagt som sim-gated slice med board/økonomi-rekalibrering.

**Goal:** Implementér den fulde sæson-model: hver pulje har PRÆCIST 140 løbsdage, der afvikles 5/dag (≈28-dages sæson), hvor et løbs etaper kan afvikles flere/dag (grand tour komprimeret til ~7 dage), højst 2 samtidige etapeløb pr. pulje, parallelle puljer i samme tier kører samme kalender (instanser), alle puljer rykker synkront (global cursor) — og board/økonomi/progression er rekalibreret fra ~60 til 140 løbsdage.

**Architecture:** Faseopdelt med hårde gates. Fase A bygger den nye afviklings-/kalender-model og afprøver den KUN på AI-divisionerne 1–3 (0 ægte spillere). Gate (simulering + ≥2 døgns fejlfri automatik). Fase B retter race_days til per-division + rekalibrerer board/økonomi/progression. Fase C udruller til Div 4–7 (39 spillere) + global synkron cursor.

**Tech Stack:** Node.js + Express, Supabase (Postgres), `node --test`. Determinisme kritisk (seed-baseret, ingen `Date.now()` i rene planlæggere) så dry-run == apply.

---

## §1. AFVIKLINGS-MODELLEN (ejeren bekræfter denne FØRST)

Dette er kernen jeg misforstod i dag. Bekræft at det er rigtigt:

1. **Sæson = 140 løbsdage pr. pulje**, afviklet ved **5 løbsdage pr. kalenderdag** ⇒ ~28 kalenderdage.
2. En **løbsdag = én etape** (endagsløb = 1 løbsdag; et 21-etapers grand tour = 21 løbsdage).
3. **Et løbs etaper er IKKE bundet til 1/dag.** På en kalenderdag fyldes 5 etape-slots; en grand tour kan fylde flere slots samme dag (op til 5 hvis den kører "solo") og fylder derfor kun ~4–7 kalenderdage.
4. **Højst 2 samtidige ETAPELØB** (fler-dages) pr. pulje — bemandbarhed: 1 trup hver. **Endagsløb kan være flere** (5 endagsløb samme dag = 5 forskellige løb; ved overgang etape→etape med endagsløb imellem kan der være >2 løb den dag).
5. De 5 daglige slots fyldes: aktive etapeløb tager flere slots/dag, endagsløb roterer ind i resten.
6. **Tier-instanser:** alle puljer i samme tier kører SAMME kalender (samme løb, samme dage), som parallelle instanser med separate felter/standings (de konkurrerer ikke mod hinanden). Div 1 = 1 instans, Div 2 = 2, Div 3 = 4.
7. **Global synkron cursor:** alle puljer rykker 1 løbsdag i takt (S2b).
8. Hver rytters arbejdsbyrde-loft: ~60 løbsdage/sæson (#1146) — relevant for binding/fyldning.

---

## §2. Hvad af dagens arbejde GENBRUGES (allerede bygget + testet)

- `raceBinding.peakConcurrentStageRaces` — concurrency-måling (in-flight-aware). **Behold.**
- Generator realistisk blanding: `divisionCalendarGenerator` (monument-min + single-kvote + exact-fit til PRÆCIST target + `allowReuseAcrossPools` for tier-instanser). **Behold — det vælger de rigtige 140 løbsdage med god blanding.**
- `divisionReset.js` + `reset-division-calendar.mjs` (ægte-spiller-guard, FK-sikker reset). **Behold som ops-værktøj.**
- `preview-calendar-rework.mjs` (read-only scorecard). **Behold til verifikation.**
- Katalog-re-kategorisering (WT-A endags-hul lukket). **Færdig.**

## §3. Hvad skal SKRIVES OM (byggede forkert i dag)

- **`planRaceSchedules` (kerne-fejlen):** i dag 1 etape/dag/spor → ~65-dages sæson. Skal omskrives til **5-etaper/dag-komprimeret**: fordel et løbs etaper over FÆRRE kalenderdage (flere/dag), højst 2 samtidige etapeløb, endagsløb fylder resten op til 5/dag, ram ~28 dage. Determinisme bevaret. (`stageRaceTracks`/`tracks`-modellen fra i dag erstattes af en slot-baseret komprimerings-model.)
- **`STAGES_PER_DAY` 2→5 + 5. slot** (`STAGE_SLOTS_CET`) — #1712's accept-kriterier. (5. slot tilføjet i dag; STAGES_PER_DAY skal til 5 globalt — men kun efter board/økonomi-rekalibrering, Fase B.)
- **`stageScheduler`:** skal kunne afvikle flere etaper fra samme løb samme dag (op til daglig cap = 5 × pulje-antal).
- **Div 1 (live nu med dagens forkerte 1/dag-model):** regenereres med den nye model i Fase A.

## §4. Hvad er NYT (ikke bygget endnu)

- **Per-division race_days** — fix cross-division-bug'en (race_days_completed=26 = sum over alle 7 puljer mod en per-division total på 60). Denormalisér til `league_divisions` (race_days_completed/total pr. pulje). Migrér forbrugere: `boardMidSeason`, `boardWeekendFinalization` (salary-cap-checkpoints), `boardRequests` (lock sidste 5 dage), `sponsorContractsService` (payout-normalisering), `economyEngine`.
- **Global synkron cursor (S2b)** — alle puljer rykker 1 løbsdag i takt; 5/dag.
- **Board/økonomi/progression-rekalibrering** fra 60 → 140 løbsdage.

---

## FASE A — Ny afviklings-/kalender-model på AI-divisioner 1–3 (0 risiko)

> Mål: bevis at 5-etaper/dag-komprimering + max 2 etapeløb + tier-instanser virker mod ægte data, uden at røre de 39 spillere.

### Task A1: Komprimeret slot-planlægger (omskriv planRaceSchedules)
- **Files:** `backend/scripts/backfillRaceScheduledFor.js` (omskriv), `backend/scripts/backfillRaceScheduledFor.test.js`
- TDD: test at en kalender med 140 løbsdage planlægges på ~28 kalenderdage med ≤5 etaper/dag, ≤2 samtidige etapeløb (via `peakConcurrentStageRaces`), og at en grand tour (21 etaper) fylder ~4–7 dage (flere etaper/dag). Determinisme: samme input → samme output.
- Implementér slot-baseret komprimering (detaljeres efter §7-svar om slot-fordelings-regel).

### Task A2: stageScheduler afvikler 5/dag inkl. flere-fra-samme-løb
- **Files:** `backend/lib/stageScheduler.js`, `.test.js`
- TDD: due-detektion + daglig cap tillader flere etaper fra samme løb samme dag; cap = 5 × aktive puljer.

### Task A3: Regenerér Div 1–3 med ny model + simulér
- Kør `reset-division-calendar` (ny planlægger) for Div 1–3. Kør `preview`-scorecard: 140 løbsdage præcist, ~28 dage, ≤2 etapeløb, blanding virkelighedstro. **Vis ejeren scorecard + dry-run før --live.**

### GATE A→B: Div 1–3 kører automatisk ≥2 døgn uden scheduler-fejl + bemandbare felter + ejer-godkendt i UI.

---

## FASE B — Per-division race_days + board/økonomi/progression-rekalibrering (rammeplan)

> Detaljeres til fuld TDD efter Fase A-gate. Rører systemer der betyder noget for de 39 spillere.

1. **Per-division race_days:** `league_divisions.race_days_completed/total`; `recomputeDivisionRaceDays`. Migrér board/sponsor/økonomi-forbrugere (per-division i stedet for sæson-globalt). Tests pr. forbruger.
2. **race_days_total 60→140** + `STAGES_PER_DAY` 2→5 globalt; re-test board-checkpoints (mid-season ved 70, lock ved 135).
3. **Sponsor/økonomi-skalering:** payout normaliseres over 140 (ikke 60). Verificér ingen utilsigtet inflation/deflation.
4. **Simulér** økonomi over en fuld 140-sæson (#extrapolate-balance-sim) før udrul.

---

## FASE C — Udrul til Div 4–7 (39 spillere) + global cursor (rammeplan)

1. **Global synkron cursor (S2b):** alle puljer rykker 1 løbsdag i takt.
2. **Migrér Div 4–7's igangværende sæson:** beslutning — re-planlæg kun kommende løb (bevar afviklede + standings + præmie) vs. fuld restart. Default-anbefaling: bevar historik, re-planlæg fremtid til ny model.
3. **board/økonomi-konsekvens-audit** for de 39 spillere.

---

## §7. ÅBNE SPØRGSMÅL (ejeren svarer før Fase A bygges)

1. **Slot-fordelings-regel:** Når 2 etapeløb kører samtidig + der er endagsløb — hvordan deles de 5 daglige slots? Fx grand tour 3 + andet etapeløb 1 + endagsløb 1? Eller en prioritet (store løb får flere slots)?
2. **Hvor hurtigt skal en grand tour afvikles?** Solo = 5 etaper/dag → 21 etaper på ~5 dage. Er ~5 dage for hurtigt for en grand tour, eller fint? (Påvirker hvor mange slots en grand tour må tage/dag.)
3. **Realtid:** afvikles de 5 daglige løbsdage over én realtids-dag (cron 5×/dag), eller hurtigere/langsommere? (Sæsonens realtids-varighed.)
4. **Div 4–7-migration:** bevar nuværende standings/præmie og re-planlæg kun fremtid, eller fuld sæson-restart for spillerne?
5. **race_days-arbejdsbyrde-loft (~60/rytter):** skal binding/fyldning håndhæve at en enkelt rytter ikke kører >~60 løbsdage/sæson?

## §8. Risici
- 5/dag rører board/økonomi/progression (Fase B, bag gate, kun efter per-division race_days-fix).
- Div 4–7 = 39 spillere (Fase C, forsigtig migration).
- Determinisme i komprimeret planlægger (test grundigt).
- Loop-guard: 2 scheduler-fejl på samme symptom → STOP + spørg.
- PR med DB-migration (per-division race_days) → ejer merger.
