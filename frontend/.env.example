VITE_SUPABASE_URL=https://example.supabase.co
VITE_SUPABASE_ANON_KEY=replace-me
VITE_API_URL=http://localhost:3001/api
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development
VITE_SENTRY_TRACES_SAMPLE_RATE=0
VITE_SENTRY_REPLAY_SAMPLE_RATE=0
VITE_SENTRY_REPLAY_ON_ERROR_SAMPLE_RATE=0.1
# Google Analytics 4 measurement ID (G-XXXXXXXXXX). Only set in Vercel
# production — leave empty locally/in previews so dev traffic never pollutes
# the data. gtag.js loads only after analytics consent (see gaIntegration.jsx).
VITE_GA_MEASUREMENT_ID=
# Ahrefs Web Analytics data-key (#5493). Cookie-free, ungated — runs on every
# page unconditionally (owner decision 22/9), unlike GA4 above. Only set in
# Vercel production; leave empty locally/in previews (vite-plugins/
# ahrefs-analytics.ts is a no-op without it, same pattern as GA4).
VITE_AHREFS_ANALYTICS_KEY=
# IndexNow key (#5493). Build-time only — used by scripts/generate-indexnow-key.mjs
# to write frontend/public/<key>.txt before `vite build` (never committed; the
# filename IS the key). Set in Vercel production once the owner generates the
# key in Ahrefs (Project settings -> Site Audit -> Crawl settings -> IndexNow).
INDEXNOW_KEY=
# Public base URL used as redirect target for password-reset emails.
# Defaults to https://cyclingzone.org if unset — set this in local dev
# to http://localhost:5173 if you want reset-links to come back to your dev server.
VITE_PUBLIC_APP_URL=https://cyclingzone.org
