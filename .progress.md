# Progress — #3232 form-nulstilling ved sæsonskifte

- [2026-08-03 START] Worktree + branch verificeret (feat/3232-form-season-reset). Issue #3232 læst (ejer-beslutning 3/8: form SKAL nulstilles, målværdi ikke valgt endnu). Starter kodeudforskning af seasonFatigueReset.js + season_fatigue_reset_enabled config-mønster.
- [2026-08-03] Kodeudforskning færdig: seasonFatigueReset.js/seasonStartHooks.js/transferPriceBand.js (multi-param app_config-mønster) + riderProgression.seededUnit (deterministisk seed) + riderCondition.nextForm læst. Kørte read-only SQL (Supabase MCP execute_sql, KUN SELECT) mod prod: nuværende form-fordeling hentet + simuleret alle 3 modes direkte i SQL (baseline/band/decay). Nøglefund: AI-ryttere sidder FAST på form=50 (sd=0, aldrig trænet), kun menneske-ryttere (n=3033) har reel spredning (sd=35.5, p10=0, p90=100). Band-mode introducerer støj i AI-formen som baseline/decay ikke gør. Går i gang med seasonFormReset.js-modulet.
- [2026-08-03] backend/lib/seasonFormReset.js implementeret (alle 3 modes + readSeasonFormResetConfig, samme fail-safe-mønster som transferPriceBand.js). 30 unit-tests grønne (inkl. idempotens-bevis for baseline+band, eksplicit IKKE-idempotens-dokumentation for decay). Wired ind i seasonStartHooks.js (ny fase season_form_reset mellem fatigue og akademi) + 7 opdaterede/nye tests i seasonStartHooks.test.js, alle grønne. seasonTransition.test.js (52 tests) kørt uændret — ingen regression (nyt hook no-op'er via tom app_config, samme som fatigue-hooket allerede gør i disse tests).
- [2026-08-03] Migration database/2026-08-03-3232-form-reset-app-config.sql skrevet (season_form_reset_mode default "off" + 5 parameter-nøgler), samme DO-NOTHING-mønster som transfer-price-band-migrationen. Sim-harness backend/scripts/formResetSimHarness.js skrevet + kørt mod ægte prod (genbruger seasonResetForm 1:1). docs/audits/2026-08-03-form-reset-sim-3232.md skrevet med fuld tabel + anbefaling (decay, med idempotens-caveat). Går videre til verify-local + commit/push/PR.
- [2026-08-03] verify-local.ps1 + nyt scripts/preflight-pr.ps1 (opdaget via CLAUDE.md-ændring midt i sessionen) begge grønne. PR #3249 oprettet som draft mod main med label backend-only + fuld template. CI-fund: 2 reelle lint-fejl fra denne PR (ubrugt destructured var + `Number(x) ?? 50`-bug i sim-harness, hvor Number() aldrig returnerer null/undefined) rettet i opfølgende commit. Sidste røde check (swallowed-catch-guard) verificeret at være FORUDEKSISTERENDE på main (identisk fejl på main's egen seneste push-CI, sporet til PR #3238/commit d2d27934) — IKKE forårsaget af denne PR. Flagget som separat task (task_2d23f69c) + PR-kommentar postet. Alle øvrige checks grønne. Session afsluttet, klar til ejer-beslutningsrunde om mode.
